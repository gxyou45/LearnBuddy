# M1 技术样板验收记录

日期：2026-09-18。

## 已完成的机器端验证

- Node.js 24.13.1，React 19.3.0，Vite 8.3.0；依赖由 package-lock.json 固定。
- TypeScript 检查与生产构建通过。
- 11 项单元／内容检查通过：课程步骤、音频引用、识别证据、重复提交、跨日期回顾、损坏存储与内容版本恢复。
- 3 条 Chrome 浏览器流程通过：完整课与刷新恢复、故事阅读与清除进度；音频加载失败、答错和跳过；320 像素窄屏与损坏数据保留。
- 主流程以 393×851 视口执行，查看了首页截图；移动视口检查不等同于安卓实机测试。
- 工作台托管 Vite 开发服务，电脑请求 http://10.127.206.238:5173/ 返回 HTTP 200。

## 尚待验证或交付

- 用户安卓手机是否可通过热点访问该地址，以及实际声音、触控、后台切换行为。
- 机器试听音频的人工审听；公开分发前的许可确认或替换为授权音频。
- 正式字表与故事审校；其余 27 字、9 次活动及完整 3 本故事尚未交付。
- 完整 1/3/7 天调度、课前回顾、多课程恢复和家长开放后续课程。
- 公网 HTTPS 预览站点；当前只有本地网络预览。

M1 为“技术样板可体验、待安卓及素材验收”；M2–M4 尚未完成。手机试用时建议先体验「认识家人」，记录是否能打开、是否有声音、按钮是否好点，以及孩子是否理解两类题目。

## 场景找字增量验证

- 13 项单元／内容检查通过，包括旧课程版本迁移和找字进度有效性检查。
- 4 条浏览器流程通过，包括逐字点选、重复点击不重复计数、提示、刷新恢复、找齐进入共读，以及 320 像素下的 52 像素点击区域和亲子跳过。
- 找字过程不增加独立识字题目的作答证据，已在完整流程中断言验证。
- 已检查 393 像素视口的场景截图；安卓真机体验仍由手机端验证。

## 一句话故事更新验证

课程结尾和书架均改为“爸爸妈妈和我一起看书。”与同一幅亲子阅读插画，播放同一完整句子音频。构建、14 项单元／内容检查及 4 条浏览器流程通过；检查了手机尺寸截图。句子中非目标字全部列入陪读字，音频仍待人工审听。

## 十课扩展验证（2026-09-18）

当前版本已完成三个主题、十课、三十字和十个图文故事的集成，替代前述样板“其余九课未交付”的旧状态。

- 18 项单元／内容测试通过：包括十课结构、30 字唯一性、每课目标字覆盖、句子与音频文本对应、所有音频引用、旧单课迁移、多课进度隔离、顺序解锁与家长权限。
- 6 条浏览器流程通过，其中一条依次完成全部十课的教学、两类练习、找字、共读与故事解锁，逐课刷新并检查完成计数。
- 单独验证家长开放不会增加已完成数量；在不同课程之间切换、刷新后恢复对应位置。
- 已查看十故事书架的整体配画；检查较长故事在 320 像素视口下不横向溢出。
- 后续状态：实际安卓逐课反馈、教学审校、人工审听与音频授权仍未完成；完整间隔调度和课前回顾未包含在本次章节扩展中。


## 2026-09-21：独立读词阶段（DEV-18）

- 十课增加独立读词步骤与阶段栏；已学词句可单独重读，书架故事提供读词入口。
- `npm test`：24 项通过；`npm run build`：通过。
- `npm run test:e2e`：9 条 Chrome 流程通过，包含十课完成、失败与跳过、旧存档恢复、只读重练、未开放链接、窄屏布局。
- 旧版找字、故事与各课快照按语义迁移，已找到的字和作答记录保留；单独重读不推进课程、不新增学习证据。
- 最终入口调整后，3 条朗读专项浏览器测试再次通过，包含书架重读返回。
- 安卓用户体验待反馈：原网址刷新后，从课内阶段栏点“读词语”；进入读词、选词、播放、返回后应仍停在原任务。完整新课在认完三个字后应自然进入读词环节。
- 逐字时间点仍为估算；找字多图与干扰字已列入开发文档第 13.7 节，尚未实现。

## 2026-09-21 · ARC-01 数据基础与 Hono 切换

- 已拆分 `apps/web`、`apps/api`、`packages/contracts`；HTTP 层使用 Hono，保留 PostgreSQL/Prisma 与 Node.js。学习页面仍使用内置课程与 localStorage，账号及云同步未接通。
- PostgreSQL 迁移通过；实际数据库与 Prisma 模型 diff 无差异。追加建表迁移修复早期空迁移，未清除运行库。
- 已入库 3 主题、10 课、30 字、30 词、10 故事、150 步、70 个题目版本、100 项素材及 3 个原型花园主题场景记录。素材和逐字时间点仍为原型审校状态；不代表六幅新找字图已完成。
- 完整构建通过；27 项单元测试通过。临时数据库 5 项集成子测试通过（Node 报告连同父测试共 6 项），覆盖新库迁移、素材 hash、Hono 接口、重复导入不覆盖、版本变化拒绝及跨档案事件/重复事件约束。
- Docker 8080 页面浏览器回归 9/9 通过，涵盖十课、词句高亮、重读、窄屏、错误播放及旧存档恢复。局域网 `/setup/` 渲染 10 个课程卡片，393px 无水平溢出，截图 `test-results/setup-mobile.png`。
- 备份保存于 `.backups/arc01-verified`；恢复到独立临时数据库及素材目录，课程/素材数量与所有素材 hash 通过，运行库未覆盖。
- 安装依赖审计为零漏洞；Prisma 工具链的间接依赖修复版本由 overrides 固定。
- 手机验收：`http://10.251.92.238:8080/setup/`，查看数据库已连接及数量，然后进入学习页测试读词、读句及找字。实际安卓手机体验待用户反馈；不是公网链接。
- 旧 5173 与新 8080 的本机存档隔离，不清除旧地址记录。下一阶段 ARC-02 才让学习页面读取内容 API。
- 容器重建验证：`docker compose up -d --force-recreate --wait` 通过；db/api/web 健康，init 退出 0，日志显示无待执行迁移及 `already imported`。重建后 100 项素材 HTTP 内容 hash 全部一致，音频 Range 请求返回 206；旧 5173 预览入口返回 200。

## 2026-09-21 · ARC-02 学习页面接入课程 API

- 首页通过 `/api/v1/catalog` 读取目录；进入课程/重读/复习时按需读取固定 releaseId 的单课包。插画、声音及逐字时间点从服务端内容包加载；前端生产产物不包含全量故事文字或原型 WAV 文件。
- 课程接口具有共享运行时校验、ETag 和 OpenAPI 文档。独立测试库验证十课内容包、无效版本/课程、304 条件响应；修改数据库课程标题后目录及内容包均返回新标题，无需重建前端。
- 完整构建通过，31 项单元测试通过；数据库集成 6 项子测试通过（含父测试 Node 报告共 7 项）。无新增数据库迁移，启动导入仍为 already imported。
- 14 项浏览器场景覆盖完成：首次全量运行 12 项通过，2 项旧断言因插画可读说明/稳定步骤 ID 变化失败；修正并保留对应验证后，在最终 Docker 8080 版本定向复验完整一课、窄屏找字、旧存档备份及清除共 3 项，全部通过。其余覆盖包括十课流程、逐字高亮、只读重读、API 失败重试、图片/音频失败、加载后 API 断开及数据损坏保护。
- 首次迁移前保存原始本机存档；新存档记录 releaseId 和 stepId。未知版本/失败加载不会静默清空进度。家长确认清除数据后，原存档和迁移备份均清除；删除失败明确提示。
- 局域网手机尺寸找字页面检查：素材 URL 为 `/media/assets/images/<hash>.svg`，无页面脚本错误、无水平溢出；目标位置仍与底图匹配。手机链接 `http://10.251.92.238:8080/` 返回 HTTP 200。
- 构建遇到容器直连 npm 超时，使用现有本机代理的可选 Docker 构建参数解决，未关闭 TLS 校验；运行服务不使用构建代理。
- 限制：实际安卓手机体验待用户反馈；学习记录仍在本机，没有账号/云同步。整份存档固定同一内容版本；跨发布版本升级、完整离线缓存及同步按后续计划实施。读词读句的时间点仍为估算值，待人工校准。

## ACC-01 家长账户与孩子档案（2026-09-21）

ARC-02 的三项手机检查已由用户确认通过。本轮完成邮箱注册／验证／重发、登录、找回密码、当前／其他设备退出，以及孩子档案创建、改名、选择和服务端归属隔离。当前所有课程权益默认免费；管理员权限不由前端或注册参数授予。

验证结果：

- TypeScript 与 Docker 生产构建通过；Compose 的 DB、API、Web、Mailpit 均健康，迁移与种子初始化成功。
- 31 项既有单元／内容检查通过。
- 隔离 PostgreSQL 集成测试 13 项通过（含两个父测试）：邮箱未验证拦截、密码验证、跨家庭读取／改名／权益拦截、Origin 校验、角色注入防护、持久化会话、退出其他设备、密码重设令牌一次性与全部旧会话撤销、限流，以及内容导入与学习外键约束。
- 16 条浏览器场景已覆盖通过：14 条课程回归、账户失败重试及完整家庭流程。实际局域网地址 `http://10.251.92.238:8080` 上先通过 15 条，修正多标签页测试的前台切换和登录标签操作后，账户两个用例最终独立复跑 2/2 通过；不是声称一次全量运行全绿。完整家庭流程包含邮箱验证、错误密码、两个孩子独立进度、刷新续学、多标签页退出、游客记录保留，以及已登录浏览器打开找回链接后重设密码。
- Prisma 实库对照 schema：No difference detected。
- `.backups/acc01-verified` 已备份并在独立数据库／素材目录恢复，恢复时含 3 个测试家庭账户、4 个孩子档案；实时数据库未被替换。备份文件权限为 600。

手机验收入口：`http://10.251.92.238:8080/#account`。邮件在电脑 `http://localhost:8025` 查看，建议注册测试地址 `xxx@learnbuddy.test`，复制验证链接到同一手机浏览器打开。Mailpit 不向真实邮箱发信，收件箱也不对局域网公开。手机实机触控和浏览器行为仍待用户反馈；生产 SMTP 与 HTTPS 不算本轮验收完成。

账户和孩子档案已存数据库。每个孩子的学习进度仍保存在当前浏览器独立命名空间，游客旧记录保留，不自动导入；ACC-02／ACC-03 分别负责云端学习记录与游客导入。内容管理与发布按计划在下一阶段推进。

## UX-01 手机单屏学习布局（2026-09-21）

根据用户“浏览器地址栏占空间，每次继续都要下滑”的反馈，学习页改为按动态可视高度排列，继续／跳过／读完按钮与中间滚动区分离。缩小重复品牌、卡片留白和插画占用，保留至少 48px 操作区域与底部安全距离；极短屏不裁切内容。

开发环境检查：9 条课程、音频／高亮阅读和短屏浏览器用例通过。393×650 的 15 个步骤正文无需滚动；320×568 的全部步骤按钮保持在屏内、页面无横向溢出。缩短到 420px 后仍可滚动中间内容，操作栏继续可见。另一个覆盖十课故事的用例通过，包括长句和陪读提示。已检查 393px 读句页截图。实际安卓／iOS 的浏览器工具栏伸缩和安全区仍需用户用原链接刷新体验确认。

部署后在实际手机地址 `http://10.251.92.238:8080` 再次运行短屏专项，3/3 通过（393×650、320×568／动态缩短、十课故事）；Compose 全部服务健康。

## ARC-03 内容管理与发布（2026-09-22）

已交付懒加载 `/admin`、草稿创建/保存、课程/词语/故事/陪读编辑、PNG 与 PCM WAV 上传、私有素材预览、逐字时间点编辑、场景位置配置、发布校验、不可变发布快照、当前版本切换/回滚及审计记录。所有后台读写与素材预览均检查管理员和会话；修改操作检查 Origin。发布时同时校验草稿与当前版本修订号，并在事务中写入规范化内容和切换指针。

自动化结果：

- 36 项单元/内容测试通过，新增文案与录音一致性、旧音频不能重新标注新文字、时间点/场景位置、上传格式与 PostgreSQL JSONB 字段排序回归。
- 隔离 PostgreSQL 测试 19 项通过（含 3 个父测试）。新增 5 组管理/发布集成用例：未登录和普通家长拦截、Origin 检查、并发保存冲突、草稿/上传不公开、非法上传/内容拒绝、故事文字与新音频一起发布、失败事务不改变指针、不泄露已拷贝但未发布素材、重复发布拒绝、旧版本读取、Range 音频、回滚与陈旧修订冲突。
- Docker 8080 入口一次全量浏览器运行 **21/21 通过**：包含原有账户、十课、重读、高亮、故障、旧存档、窄屏，以及 2 项新增后台界面流程。新增后台界面测试使用模拟管理 API，真实鉴权与发布事务由隔离 PostgreSQL 集成测试验证；不将它们表述为真实浏览器跨服务发布验收。检查了手机尺寸后台预览截图 `test-results/admin-preview.png`。
- 修正验证过程发现的 JSONB 对象字段排序误判，并增加回归测试。数据库字段删除行为在 Prisma 中显式声明为 NoAction，与追加 SQL 迁移保持一致。
- 用户指定的 `gxyou45@gmail.com` 已确认邮箱验证通过，并通过维护命令授予管理员；未读取或重置密码。管理员可在账户页进入内容工作台。

入口：`http://10.251.92.238:8080/admin`，仅本地网络。手机验收建议：登录指定账户 → 新建草稿 → 改课程名称并保存 → 校验/预览 → 发布 → 用新孩子档案查看新版本 → 在后台启用原版本。要修改故事或词语，请同时替换相应音频及时间点；原有学习档案始终保留原内容版本，不清除旧进度。

状态为“待验证”：实际用户手机编辑/发布体验、素材人工审校及审听仍待完成。全部素材仍为待审，当前发布用于内部体验。没有新增六幅找字图，也没有实现云端学习记录；下一项为 ACC-02。

最终部署检查：Compose 的 DB/API/Web/Mailpit 全部健康；完整 Docker 生产构建通过。数据库与 Prisma diff 为 `No difference detected`。变更前备份位于 `.backups/pre-arc03-20260922`，最终备份位于 `.backups/arc03-verified`；最终备份在独立库/素材目录恢复通过，包含 9 个家庭账户、15 个孩子档案、当前版本 `prototype-v4`，实时库未被替换。当前体验库没有测试发布或草稿，恢复检查中草稿数为 0；发布后的版本和上传文件验证覆盖于隔离集成测试。

最终镜像部署后，后台 2 项浏览器用例定向复验 2/2 通过。实际局域网 `/`、`/admin`、`/api/health` 均返回 200，健康阶段为 ARC-03；未登录后台目录接口返回 401。默认目录仍为原 `prototype-v4`，十课完整保留。


## ACC-02 在线学习记录与错题（2026-09-22）

已交付登录孩子的云端课次、继续位置、真实选项与答案、服务端判题、提示/跳过/音频状态、找字结果、能力摘要、错题分页和基础次日复习。正式进度与累计完成分开保存；只读重读和回顾均不推进正式课次。网页写入需服务端确认，断网暂停操作，响应丢失可使用同一事件重试。游客行为保留；旧本机存档原样保留，不自动导入。

自动化结果：

- 单元/内容测试 **43/43** 通过，覆盖辅助证据排除、跨日规则、时区、伪造 correct 拒绝、完整音频结束和取消，以及切换孩子/版本时迟到课程请求不覆盖新内容。
- 隔离 PostgreSQL 集成测试 **27/27** 通过（含 4 个父测试）。新增 7 组在线学习验证，覆盖家庭/孩子隔离、Origin、并发重试、载荷冲突、修订号、实际作答与选项、辅助记录、跨实例恢复、累计完成、独立复习位置、错题历史和失败事务原子性。
- Docker 8080 一次全量浏览器回归 **23/23** 通过，包括原有十课/后台/账户/短屏流程及 2 项云端用例。云端用例使用真实注册、验证邮件、登录和学习 API，在两个独立浏览器上下文续学；记录错答后刷新保留实际选择和选项顺序；只读重练不改修订号；保存成功但丢失响应后重试不多推进；断网暂停写入，旧本机原始记录保持一致。
- 浏览器回顾部分仅模拟次日到期队列，真实后端负责启动复习、保存答案及课程位置；真实到期队列查询由数据库测试覆盖。没有将当天浏览器测试表述为跨真实自然日验收。
- 生产构建、Compose 部署与健康检查通过，局域网 API 健康阶段为 ACC-02；Prisma 与数据库 diff 为 `No difference detected`。核对手机尺寸家长错题截图 `test-results/cloud-mistakes.png`。
- 迁移前备份 `.backups/pre-acc02-20260922`，验收备份 `.backups/acc02-verified`。独立数据库与素材目录恢复通过，验证了发布素材、课次/孩子版本、实际答案判定及错题引用，未替换体验库。保留现有管理员、已发布内容和草稿。

手机体验入口：`http://10.251.92.238:8080/#account`。登录 → 选择孩子 → 学几步并作答 → 另一台设备登录同一账户和孩子继续 → 家长中心查看错题。只读“读词语／读句子”返回后仍在原课次位置；新学字次日进入回顾花园。

状态为“待验证”：用户真实安卓/其他设备登录续学及音频播放仍待体验；自动化跨浏览器不代替实机。当前按整个孩子档案固定课程版本，没有离线持久队列、旧记录导入或完整 1／3／7 日调度；分别留给 ACC-03 和 DEV-22。声音及家长观察偏好仍是本机设置。

全量回归后，加强了复习用例：等待正式切题确认后读取题目编号，逐题断言答对，并确认历史错题次数不增加。云端专项再次运行 **2/2 通过**。

## ACC-03 弱网同步与旧记录导入（2026-09-22）

已交付服务端登记的同步流、预生成题目呈现、有序批量接收/应用回执、修订号增量读取和替换课次的移除标记。网页使用 IndexedDB 持久化事件编号、顺序、已确认快照与本机预览，落盘失败不推进；离线继续已缓存课次，联网或原账号重新登录后补传。多设备冲突保留原始提交，由用户明确选择保留副本并按云端继续。旧记录从家长中心预览、选目标并确认导入，原存档不删除。

后端与单元验证：

- 单元/内容测试 47 项通过，新增本机预览与云端快照分离、未登记题目不能离线推进、账号/孩子存储键隔离、协议边界和不可信离线日期不能制造跨日掌握。
- 隔离 PostgreSQL 集成测试 37 项通过（含 5 个父测试）。新增 9 组同步/导入用例：归属与 Origin、乱序等待、并发重试与同编号冲突、陈旧课次保留原始事实、增量空响应/补丁/未知游标重置、先提示后作答、失败回滚但保留拒绝记录、旧存档位置映射及去重、迟到作答时间边界、重玩移除旧继续位置但保留历史。
- Prisma 与已部署库 diff 为 `No difference detected`。新增迁移 `202609220003_offline_sync`，迁移前备份 `.backups/pre-acc03-sync-20260922`；未清空或替换用户数据，保留管理员、已发布版本及草稿。

浏览器验收使用实际 Docker API、PostgreSQL 和 Mailpit。新增 5 项场景：断网作答/离线刷新/联网只补传一次、多设备冲突与明确继续选择、家长摘要确认导入、IndexedDB 写入失败不假报保存、退出及另一个家庭登录期间队列归属不变。已有云端丢失响应测试改为批量接口；继续验证实际选项与服务器确认，不把本机预览当作云端成功。注册和登录测试在真实 429 限流后等待窗口重试，没有降低服务端保护。最终全量结果另附。

验收边界：自动化使用两个独立浏览器上下文，不等同于真实安卓/跨手机验收。HTTPS/localhost 支持应用外壳离线重开；当前手机 HTTP 局域网入口只承诺已打开页面离线继续和联网重开后的队列恢复。离线素材未缓存时允许辅助/跳过；新建课次需联网。事件报告时间与接收时间偏差达到两分钟时，不据此变更已有掌握或制造跨日证据。同步冲突保留副本而不自动合并争议作答；摘要增量为改变字段/集合整体替换，尚未提供账号/档案删除功能（属于 R7）。DEV-22 课末综合练习未在本轮实现。


ACC-03 最终结果：最后一次连续执行的单元测试 **47/47**、生产构建、Docker 部署及完整浏览器回归 **28/28** 全部通过；数据库专项为 **37/37**。已检查 `test-results/offline-queue.png`：离线刷新后实际答案、待同步数量及底部继续按钮显示正常，393px 视口按钮未超出可视高度。局域网健康接口返回 ACC-03，DB/API/Web/Mailpit 均健康。

最终备份 `.backups/acc03-sync-verified` 在独立数据库及素材目录恢复成功：45 个学习课次、25 条作答、9 项错题、75 个同步流、3 份旧记录导入、162 份增量摘要快照；原有发布版本及 1 份草稿均保留。验证逐条同步状态、序号连续性、孩子/课次归属、导入来源与指纹及快照修订一致，体验库未被替换。状态记为“待验证”，仅剩实际用户手机/HTTPS 环境验收，下一开发项为 ARC-04／ACC-04；没有自动导入用户本机存档。

## R7／ARC-04 + ACC-04 本地数据管理验收（2026-09-22）

用户确认当前没有域名、正式部署和邮件环境，本轮范围为本地开发与验证；正式 HTTPS 手机、真实邮件及安卓双设备验收仍待环境。未创建外网资源，DEV-22 课末综合练习仍按原计划安排。

入口：`http://localhost:8080/#account`（手机使用电脑局域网 IP）→「导出与删除数据」。家长可选择家庭或单个孩子导出 JSON；文件区分云端记录与当前设备待同步/冲突副本，不含密码、登录令牌或清理凭据。删除要求输入精确邮箱/昵称并核验当前密码；孩子删除保留兄弟姐妹，账户删除撤销全部会话，共用课程作者关联匿名化。

本地体验步骤（删除验证仅使用专门创建的测试档案）：

1. 登录并展开数据管理，选择孩子，导出并检查其课程版本和学习记录。
2. 断开同步后产生一条学习事件，导出应保留未确认的本机副本，云端位置不虚报前进。
3. 输入错误昵称或密码，确认无法删除；正确确认后该孩子消失，其他孩子和游客记录保留。
4. 第二个浏览器离线保留待同步记录，在第一个浏览器删除目标后重新联网；对应本机队列清除，旧补传不能重建档案。
5. 测试账户删除后，另一浏览器的会话失效；清理凭据只返回删除状态，不能访问学习数据。

已完成恢复演练：先为新建临时家庭备份 `.backups/r7-before-fixture-deletion`，随后只删除该测试家庭，再在隔离库恢复删除前备份。重放 0 条归档凭据和 1 条最新外部凭据后，测试家庭不再存在，恢复库 43 个旧会话已撤销；原库及其会话未替换。升级前 `.backups/pre-r7-data-management` 也成功追加 R7 迁移并恢复，保留当时 45 个课次、25 条作答、9 项错题、75 个同步流、3 份导入、162 份增量快照、40 个账户、50 个孩子及 1 份内容草稿。

回归发现并修复：增量读取曾在 RepeatableRead 事务中调用空更新 upsert 写入快照，多页面首次读取可能触发 `ProgressSnapshot_pkey` 冲突并显示课程加载失败。现将增量读取改为只读；快照由持有孩子锁的学习写入生成，缺失基线返回全量。新增并发首次读取及已有修订号读取测试，断言读取不新增快照、不更改学习事实。

最终回归与备份结果在本节下方记录，正式手机验收不以自动化浏览器替代。

最终自动化结果：生产构建与本地 Compose 更新成功；单元/内容测试 **47/47**、隔离数据库集成测试 **47/47（含 6 个父测试）**、完整浏览器回归 **31/31** 全部通过。数据库测试包含导出归属/事实与凭据排除、删除密码/Origin/目标核验及限流、兄弟姐妹与共用内容保留、全部会话撤销、失败/中断删除凭据重试重放、生产 Cookie 策略，以及新发布后原版本续学和首次并发增量读取。浏览器新增三个数据管理场景，实际下载文件、验证未同步副本和云端位置分离，两个独立上下文验证删除孩子/账户后重新联网清理。

已检查手机宽度数据管理截图 `test-results/data-management-mobile.png`，页面无横向溢出，删除按钮在确认文字不匹配时禁用。原有十课流程、音频重读、离线刷新、冲突副本、导入、存储失败、账号切换及窄屏操作均通过。Prisma 结构比对为 `No difference detected`（本轮最后修复未更改数据库结构）。

最终备份 `.backups/r7-local-verified` 已在独立数据库和素材目录恢复通过：重放 **9 条归档 + 9 条最新外部删除凭据**，撤销恢复库 81 个旧会话；验证 79 个课次、40 条作答、15 项错题、159 个同步流、6 份导入、327 份快照、73 个账户及 85 个孩子，保留 1 份内容草稿。数量包含自动化创建的测试档案，恢复没有覆盖运行库。数据库、API、Web、Mailpit 均健康；健康接口返回 R7-local 和当前活动发布版本。指定的已验证管理员权限保留。

本轮本地开发及验证已完成。统一状态仍为“待验证”，仅保留正式 HTTPS/真实邮件和用户安卓实机验收；无已知未解决的本轮本地回归失败。下一计划开发项为 DEV-19 音频时间点校准，DEV-22 综合练习仍按既定顺序安排。

## 完整 1000 字课程本地发布（2026-09-22）

- 当前内容版本：`curriculum-1000-v1`。保留原十课的 ID、步骤、字词和媒体，追加 C007—C200；合计 204 课、1000 个不重复汉字、2000 道辨认题、2632 条素材引用、50 次主题复习和 10 次阶段共读检查。
- 新课五字可分次学习，找字场景提供五个独立位置。词语录音使用真实 `audioText`，同词的两个字不互作干扰项。新课配图为原创 SVG 图文共读卡，语音为 macOS Tingting 合成、时间点为估算，未声称已完成专业人工审校。
- 共读后可进入综合练习：本课至多两个字，已学历史错题／到期重点至多三个字，同字去重。新课理解问答、主题复习与阶段检查已在课程内显示；旧十课补充理解问答，保持原内容版本不变。
- 严格追加版本可扩展云端与访客目录。旧课次、题目编号、位置和答案不重写；旧离线流仍可补传，复习旧题不会把档案版本降回旧目录。管理员邮箱的验证与角色仍正常，现有一个孩子档案通过兼容性检查。
- 校验：49 项单元测试通过；52 项独立数据库测试通过。浏览器首轮 34 场景中 32 通过，逐一验证了全部 204 课内容包、故事音频 Range 与图片；另外两项为小屏问答布局和一次账户加载失败。账户切换连续两次单独重测通过；小屏问题修正后另行复验，结果见下方最终补记。新增五字流程与最终单元复测通过。
- 发布前备份 `.backups/pre-curriculum-1000`；发布后备份 `.backups/curriculum-1000-published` 已在隔离库和临时媒体目录恢复成功，全部版本和媒体 SHA-256 通过。重放 12 条备份凭据与 12 条最新删除凭据，撤销恢复库 96 个旧会话；核验 93 个正式课次、45 条作答、17 个错题项、192 个同步流、7 份旧导入和 382 个进度快照。真实数据和当前登录不受影响。
- 运维实现和重建／回退流程见 [课程发布手册](scripts/curriculum/README.md)。API、网页、数据库均健康。手机入口保持 `http://10.251.92.238:8080/`，电脑 `http://localhost:8080/`。
- 发布范围为本地体验；正式 HTTPS／公网环境、专业人工审校与儿童试读仍待开展。

### 完整课程最终补记

最终部署后，393×650、320×568 的学习操作可见性，以及原十课／各阶段长共读材料的可读性三项浏览器测试全部通过（3 passed，58.2 秒）。旧课问答默认折叠，短屏故事插图调低；较长的新共读材料允许内容区滚动，操作按钮保持可见。此前账户切换失败连续两次单独重测通过，未发现记录丢失；首次加载失败原因未稳定复现，不将其宣称为已定位的代码缺陷。

最终只读核验：健康状态 `ok`，当前版本 `curriculum-1000-v1`，204 课、1000 个不重复汉字，新增课程升级和综合复习参数均已列入已部署 OpenAPI 文档。此次发布所需的本地开发、内容发布、自动验证及备份恢复工作已完成。
