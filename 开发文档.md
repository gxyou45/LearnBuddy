# LearnBuddy 汉字小屋：开发文档

版本：v0.4 ｜ 日期：2026-09-21 ｜ 对应 [产品设计方案](./产品设计方案.md)

当前执行顺序、依赖、工期与状态统一维护在[开发计划](./开发计划.md)。本文件第 13.1–13.6 节为首版历史计划与记录，第 17 节为增量实现规格；历史估算不与新计划相加。

已确认架构（2026-09-21）：React/Vite + Hono on Node.js + Prisma + PostgreSQL + 对象存储适配器 + Docker Compose，详见[前后端分离选型方案](./前后端分离选型方案.md)。同一前端提供学习端与懒加载 `/admin`；未来小程序/App 共用 API。ARC-01 已完成 Hono 切换及数据基础自动验证，待用户手机验收；网页现已接课程 API，进度仍保存在本机。后续规格以本文新增“多客户端实施约定”和统一计划为准。

## 1. 开发目标与边界

开发适配安卓竖屏浏览器的网页首版：3 个主题、10 次活动、30 个目标字、10 个一句话共读故事。首先交付第 1 次活动的完整闭环，再填充内容。

当前已建立 React + TypeScript + Vite 工程，并实现十课完整交互内容；构建、单元测试及十课浏览器流程通过，本地预览已启动。正式课程素材审校、安卓真机和公网部署尚未完成。实际运行方式见 README.md。

当前学习页面已接通课程 API，学习数据仍在本机保存。教学内容和素材通过数据库及文件存储提供；儿童录音、支付、实时 AI 不因此自动进入范围；用户随后明确要求的账户、进度及错题云存储见第 13.8 节。

## 2. 已有网页原型技术基线

- 前端建议采用 React + TypeScript，使用 Vite 构建，普通 CSS 实现布局及少量反馈动画。
- 内容使用 JSON 或 TypeScript 数据文件，图片和标准音频作为静态资源。
- 使用 localStorage 保存小规模进度；通过统一仓储接口访问，后续可以替换为 IndexedDB 或云同步。
- 使用 HTML 音频播放能力，封装播放器；不把浏览器语音合成作为标准读音来源。
- 首版使用静态部署。可采用 hash 路由，避免静态托管没有页面回退规则时刷新子页面失败。
- 动画服从用户减少动态效果设置；不以大型游戏引擎作为首版依赖。

初始化时确认依赖相容性、运行环境及锁文件；在 README 记录实际版本和最终运行命令。

## 3. 当前工程与目标目录

```text
apps/
  web/                 # 已拆分：React/Vite 学习页面与 public 素材
    src/admin/         # 规划：懒加载 /admin，单独布局及角色检查
    src/platform/      # 规划：网页音频、缓存、网络、认证适配
  api/                 # 已拆分：HTTP 层已切换 Hono，Node.js 运行
    src/modules/       # 规划：accounts/content/learning/entitlements
    prisma/            # 数据模型及已验证迁移
packages/contracts/    # 已有共享类型及内容校验；继续补齐 API 契约
scripts/import-content/# 现有原型导出及导入工具
infra/                 # 同域反向代理配置
compose.yaml           # 已验证一键启动与健康检查
tests/                 # 单元与浏览器流程测试
```

小程序/App 工程在渠道选定后再增加。当前 `apps/web/src/content.ts` 仅用于原型与导入来源，ARC-02 完成后学习运行时通过 API 取内容。目录规划不代表其中所有模块已经建立。

## 4. 数据模型

避免把“课上完了”和“字会认了”存成同一个布尔值。

| 实体 | 必要字段 | 说明 |
| --- | --- | --- |
| Character | id、text、reading、examples、audioId | 同一字在具体例词中的读音另行配置 |
| Theme | id、title、lessonIds、storyId | 主题与活动关系 |
| Lesson | id、themeId、targetIds、steps、contentVersion | 每次 3 个目标字，步骤可配置 |
| Step | id、type、targetIds、prompt、options、answer、hint、audioId | 根据题型区分必填字段 |
| Story | id、pages、requiredLessonIds、supportCharacters | 明确家长陪读字 |
| Attempt | id、sessionId、stepId、characterId、taskType、correct、hintUsed、firstTry、timestamp、localDate | 每次回答记录，不只保留最终答对结果 |
| CharacterProgress | characterId、status、lastSeenAt、nextReviewDate、reviewStage | 掌握与回顾状态 |
| LessonProgress | lessonId、status、stepId、contentVersion | 未开始、进行中、完成 |
| ParentObservation | characterId、result、observedAt | 亲子认读记录，独立于自动题目证据 |
| Settings | soundEnabled、parentUnlockedLessonIds | 家长设置 |
| Asset | id、path、type、source、license、reviewStatus | 素材来源与审校情况 |

给主题、字、活动和步骤分配稳定 ID，不使用数组下标作长期标识。课程变化时通过 contentVersion 检查恢复位置是否仍有效；无效时回到本次活动开头，保留已完成课记录。

## 5. 活动状态机

```text
目标流程：可选回顾 → 情景 → 认字 → 读词语 → 练习／迁移 → 找字 → 读句子 → 综合练习（可跳过） → 完成
                           ↑ 提示／重听     |
                           └───────────────┘
任一步骤 → 退出并保存 → 地图 → 继续未完成活动
```

具体规则：

- 每次提交答案或推进步骤后保存，不能仅依赖页面关闭事件。
- 答案首次提交后短暂锁定按钮，防止双击重复记账；使用唯一事件 ID 去重。
- 错误不推进掌握证据；提供提示后可继续，连续困难允许跳过。
- 跳过与答错分别记录，均不计为独立答对。
- 音频结束不是通关条件，播放失败时仍可由家长陪读继续。
- 离开页面、切换步骤和进入后台时停止当前语音；返回时等待主动点击重播。
- 完成活动只更新完成状态、后续入口和奖励，不直接把所有目标字标为较稳定。

## 6. 掌握规则与回顾算法

### 6.1 初版确定性规则

规则参数集中配置，并在家庭试用后调整：

1. 仅有教学曝光时为“已接触”。
2. 存在识别练习记录后为“练习中”。
3. 最近最多 6 次有效任务中，至少 3 次首次、无提示答对，分布在至少 2 个本地日期且覆盖至少 2 类任务，最近一次也答对，才标记“较稳定”。
4. 有效任务包括听音选字、见字选场景等经过审校的辨认任务；教学中的跟随点击、故事翻页不计入。
5. 同一会话中同一题的重复尝试只贡献一次独立成功证据。连续两次独立任务错误时，“较稳定”回到“练习中”，界面不使用惩罚语言。

记录 UTC 时间戳以及作答时的本地日期。不能用 UTC 日期直接代替本地学习日。日期只用于粗粒度调度，不用于高风险决策。

### 6.2 回顾安排

初次练习后默认次日回顾，后续成功可尝试按 1、3、7 天递增间隔；错误后改为次日再见，当前活动中最多追加一次有帮助的重现。

- 每次学习开头最多 2 个到期字，优先最近错误、其次最久未见。
- 到期字较多时提供“今天先复习”的入口；不强制清空才能学习。
- 单独回顾一次最多 5 个任务，可随时结束。
- DEV-22 在读句子后增加综合练习：通常 3–5 题，本课重点 1–2 题＋历史错题/到期项 2–3 题；候选不足不凑数，首版听音选字与看字选意思。三个入口共用选题和记录，按目标/题型去重，刚练过的题不立即重现。
- 课末练习可跳过或陪做，不影响课程完成、解锁及正式位置；只读重读不触发练习。总时长通过替换重复课内题、缩短找字控制。详细验收以开发计划 DEV-22 为准。
- 掌握状态与回顾阶段分开计算；较稳定的字仍会进入回顾。
- 没有到期项目时显示可返回地图的空状态。

本规则只是初版产品启发式，不宣称为已验证的记忆模型。

## 7. 本地持久化

使用带版本的键，例如 `learnbuddy:v1:progress`，保存 schemaVersion、内容版本、活动进度、字状态、作答证据和设置。

- 读写均捕获异常；JSON 损坏时不让整个页面崩溃，提供明确的恢复提示。
- 存储被禁用或容量不足时允许继续当次活动，家长页提示“本次进度可能无法保存”。
- schemaVersion 升级时提供迁移函数，不静默清空旧进度。
- 不把图片和音频写入 localStorage。
- 每字保留有限的近期证据，例如 30 条，并保存必要的汇总状态，避免无限增长。
- 清除进度仅在家长区执行，需二次确认，并清楚说明影响。
- 首版单浏览器按单儿童使用；多标签页不同时学习，可检测 storage 变化后提示刷新。

localStorage 按网站来源隔离：协议、域名／IP、端口变化都会影响数据可见性。热点地址改动、切换在线站点不会自动迁移原进度。无痕模式不作为持久保存环境。

## 8. 音频与内容管线

音频在用户主动点击“开始”或语音按钮后播放；捕获播放失败并显示可重试入口，不依赖自动播放。

播放器全局只允许一个语音播放，重复点击同一按钮从头重播，切换任务取消上一段。只预加载当前活动必要资源，避免移动数据下整套音频一次下载。

内容流程：字表与故事共同编排 → 人工审核字形、读音和句意 → 制作素材 → 审听与授权检查 → 数据校验 → 真机体验。

构建前内容校验至少包括：

- ID 唯一，引用存在；最终目标字去重后为 30 个。
- 10 次活动、3 个主题和10 个一句话故事的关联完整。
- 题目恰有一个预期正确答案，选项没有重复导致的歧义。
- 音频与图片路径存在，发布素材的审校状态通过。
- 故事汉字能映射到此前目标字或显式列出的陪读字；该检查不能代替人工句意审校。
- 同一目标字有至少两类有效任务，支持掌握规则采集证据。

原型阶段若缺少正式音频，可用明确标识的占位资源开发流程；进入家庭试用前必须替换并审听。

## 9. 安卓网页适配

- 配置移动 viewport，禁止横向溢出，保留用户缩放能力。
- 以 360–430 CSS 像素宽为主要设计范围，并检查 320 像素窄屏。
- 控件至少 48×48 CSS 像素；文字不使用图片替代，字卡需保证字形完整。
- 优先点击操作；语音按钮提供可访问名称，选项使用真实按钮元素。
- 页面高度兼容浏览器地址栏展开／收起，底部按钮留安全间距。
- 字体选用字形清楚的中文系统字体；若引入字体文件，先确认授权、字形和下载体积。
- 不依赖 hover、颜色或动画单独传达信息。
- 有网但资源失败时显示重试；无网时明确状态，首版不承诺完整离线可用。

验收以用户的实际安卓手机浏览器为优先，另外检查 Android Chrome。系统自带浏览器型号在真机阶段登记；微信内置浏览器属于补充检查，出现差异可提示在系统浏览器打开。

## 10. 开发命令与热点预览

以下命令已在当前项目提供；npm test 与 npm run build 已验证。完整浏览器流程测试使用 npm run test:e2e，需要预览服务已启动。

```sh
npm install
npm run dev -- --host 0.0.0.0 --port 5173 --strictPort
npm run build
npm run test
```

### 10.1 本任务中的持久进程约定

依据工作区约定，实际需要开发服务器时使用公开工作台命令管理，不在 agent 窗口启动独立后台进程：

```sh
tmux-agent-workbench run --name dev /Users/sophia.gao/Workspace/CodexProj/LearnBuddy -- npm run dev -- --host 0.0.0.0 --port 5173 --strictPort
```

保存命令返回的 pane ID，并用 `tmux capture-pane` 查看输出。若工作台会话无法解析或 socket 权限受限，应按环境权限流程解决，不能直接另起后台进程。上面的直接 dev 命令供用户手工终端运行或说明底层命令使用。

### 10.2 手机热点访问步骤

1. 手机开启热点，电脑连接该热点，保持两者连接。
2. 开发服务器监听 `0.0.0.0:5173`；`0.0.0.0` 是监听地址，不是手机要打开的地址。
3. 在电脑网络设置中确认当前 Wi-Fi 接口的 IPv4 地址。macOS 可先用 `networksetup -listallhardwareports` 确认 Wi-Fi 接口，再用 `ipconfig getifaddr <接口名>` 查询；不要默认接口一定是 en0。
4. 电脑先打开 `http://localhost:5173`，再打开自己的局域网地址，确认服务正常。
5. 安卓手机浏览器输入 `http://<电脑的热点网络IP>:5173`。此处 IP 不是手机的网关地址，也不是 `localhost`。
6. 如果电脑系统弹出防火墙提示，只允许本次开发服务必要的入站访问，不关闭整个防火墙。

排查顺序：服务是否运行 → 端口是否正确 → 是否监听所有接口 → 电脑 IP 是否变化 → 防火墙 → 热点是否限制手机访问客户端。

热点手机能够访问其客户端取决于机型与网络行为，不能保证。若失败，交付静态构建并部署到 HTTPS 预览站点；用户通过流量访问链接。不要通过路由器端口映射把开发服务器直接暴露到互联网。

## 11. 在线预览与发布

构建产物通常为 `dist/`，在项目初始化后确认。选择支持 HTTPS 的静态托管服务，上传构建产物并进行手机验收；具体服务和账号尚未确定。

- 部署前完成构建和资源路径检查，确认没有密钥或个人数据打包到静态资源。
- 若部署在子路径，配置正确资源基础路径；hash 路由应能直接打开及刷新。
- 内容文件版本化，避免更新课程后旧恢复位置失效。
- 首版不添加 Service Worker，减少过期缓存导致的调试问题；后续按需评估安装及离线能力。
- 交付实际链接和可选二维码；在链接实际发布、访问验证前不宣称已上线。

## 12. 验证计划与验收清单

### 12.1 自动化验证

单元测试聚焦业务规则：跨日期和跨题型掌握判断、提示及重复点击不计独立证据、错误回退、回顾排序与上限、存储迁移和损坏恢复。

流程测试至少覆盖：首次进入并启用声音 → 完成活动 → 解锁下一活动 → 刷新恢复；中途退出恢复；音频失败后继续；在家长页确认清除数据。

执行类型检查、构建和内容引用校验。页面布局和声音体验仍需真机检查，不能只靠自动化证明。

### 12.2 安卓人工验收

- [ ] 用户热点场景可访问；若受限，有经过验证的在线替代链接。
- [ ] 首次点击后声音可播放，重听不会叠音，进入后台停止播放。
- [ ] 页面无横向滚动，大字不截断，底部按钮不被浏览器工具栏遮挡。
- [ ] 选择错误可获得提示并继续，不发生无限重试。
- [ ] 重开浏览器后同一地址的进度仍存在。
- [ ] 断网或单个音频失败可重试，流程不出现空白页。
- [ ] 更改地址导致存储隔离的限制在家长页明确说明。
- [ ] 10 次活动、10 个一句话故事可完整体验，资源与读音已人工审校。
- [ ] 家长调整入口、音量开关、清除进度行为符合说明。

## 13. 开发计划索引与首版历史记录

### 13.1 排期假设与分工

按 **1 名前端开发者、10 个工作日实现与联调、另预留 2 个工作日缓冲**安排首版。此为工作量估算，不是已承诺的交付日期；D1 指实际开始开发的第一个工作日。已启动开发，当前状态见第 13.5 节；D1–D10 表为原始估算，不代表必须等待对应自然日才可推进。

排期成立的条件：采用可复用的轻量插图和互动模板，不制作逐字定制动画；第 1 次活动素材在 D2 前可用，完整课程、故事及已审校音频在 D7 前可用。外部录音、插画制作、内容审校的等待时间和后续两周家庭试用不包含在开发工期内。D3 样板完成后，依据实际反馈重新检查剩余估算。

| 角色 | 责任 | 参与时点 |
| --- | --- | --- |
| 开发执行者 | 页面、交互、内容集成、状态规则、测试、运行文档 | D1–D10 |
| 内容审校者（人选待落实） | 审核字表、例句、题目、故事、音频和素材使用许可 | D1–D2 样板；D4–D7 完整内容 |
| 产品负责人／用户 | 体验样板，反馈孩子使用问题，参与安卓真机验收 | D3、D8、D10 |

角色不代表必须配置三名全职人员，但内容审校必须有人负责。开发可继续完成不依赖素材的功能；未审校内容不能进入家庭试用。

### 13.2 每日任务与交付

| 时间 | 任务编号与具体工作 | 依赖 | 当日交付与完成标准 |
| --- | --- | --- | --- |
| D1 | DEV-01 初始化工程、脚本、类型检查和路由；DEV-02 建立颜色、字号、按钮与移动布局；DEV-03 启动热点预览 | 开发环境可用 | 安卓打开欢迎页和地图骨架；记录浏览器、IP、访问结果；网络受限时明确在线替代路径 |
| D2 | DEV-04 定义内容模型并录入第 1 次活动；DEV-05 完成字卡、音频播放器、两类题目组件 | DEV-01、样板内容和音频 | “我、爸、妈”可展示、重听和练习；音频失败有提示；占位素材明确标记 |
| D3 | DEV-06 串联活动状态机、共读片段、结束奖励；DEV-07 实现本机保存与中途恢复 | DEV-04、DEV-05 | 完成 M1：手机能完成一节课，退出或刷新后恢复，用户可实际体验 |
| D4 | DEV-08 实现掌握状态和回顾调度纯函数；补充跨日期、提示、重复尝试和错误回退测试 | DEV-07、作答记录模型 | 规则测试通过；单次答对不会直接显示较稳定 |
| D5 | DEV-09 完成回顾花园、到期队列和空状态；DEV-10 实现地图解锁、重玩及固定装饰 | DEV-06、DEV-08 | 活动完成与识字状态分开；薄弱字可回顾，错误不阻断课程 |
| D6 | DEV-11 完成书架与故事播放器；DEV-12 完成家长进度、开放入口、声音设置与清除确认 | DEV-07、DEV-09、DEV-10 | 完成 M2：全部核心功能可用，支持家长陪读及认读记录 |
| D7 | DEV-13 集成其余活动、10 个一句话故事、音频和插图；DEV-14 完成内容引用与资源校验 | DEV-05、DEV-11、完整素材审校完成 | 完成 M3：30 个目标字、10 次活动、10 个一句话故事齐备，无缺失资源与未标注陪读字 |
| D8 | DEV-15 安卓真机测试触控、音频、后台切换、窄屏及网络失败；补充核心流程自动化测试 | M2、M3 | 第 12 节清单有逐项结果；记录缺陷严重度、复现步骤和处理状态 |
| D9 | DEV-16 修复验收缺陷、优化当前活动资源加载；完成构建和回归 | DEV-15 | 无阻断学习的问题，关键规则和流程测试通过，正式构建成功 |
| D10 | DEV-17 整理 README、素材清单、已知限制、验收记录与预览说明；按选定方式交付访问入口 | DEV-16；在线部署时需托管账号可用 | 完成 M4：用户手机可访问首版并完成活动，交付源码和运行说明 |
| D11–D12 | 仅处理音频兼容、真机缺陷、内容返修等缓冲工作 | 实际剩余问题 | 达到 M4 标准；如仍受外部资源阻塞，明确未交付项和调整后的排期 |

D4–D7 期间可由内容审校者独立完成内容准备，开发执行者继续实现通用功能。功能模块应持续集成和验证，不把测试全部推迟到 D8。

### 13.3 里程碑与用户可见结果

| 里程碑 | 目标时间 | 用户能体验什么 | 验收门槛 |
| --- | --- | --- | --- |
| M1：可玩的样板 | D3 | 地图 → 第 1 次活动 → 共读片段 → 装饰奖励 → 返回 | 安卓完成闭环，声音可重听，进度可恢复；样板反馈整理成问题列表 |
| M2：功能完整 | D6 | 回顾、地图解锁、书架及家长页 | 状态更新正确，困难时能继续，家长设置有效 |
| M3：内容完整 | D7 | 10 次活动、30 字、10 个一句话故事 | 内容、音频、引用和许可检查完成；无发布用占位素材 |
| M4：可交付试用 | D10，必要时使用缓冲 | 在自己的安卓手机上持续体验完整首版 | 构建和关键测试通过，第 12 节验收完成，运行方式和已知限制有文档 |

M1 后优先修正儿童无法理解的操作，再扩展相同交互到全部课程。无需为每个日常实施步骤另行等待确认；涉及目标范围变化的反馈要更新本文与产品方案。

### 13.4 关键依赖与延期处理

| 风险或依赖 | 最晚检查点 | 处理方式 |
| --- | --- | --- |
| 热点无法访问电脑 | D1 | 尽早验证静态在线预览路径，不等到交付当天才排查 |
| 第 1 次活动音频或内容未完成 | D2 | 用标记清楚的占位内容完成技术闭环；正式声音体验和 M1 内容验收顺延 |
| 全量素材或审校未完成 | D7 | 继续测试已有内容，提供明确标为部分内容的预览；M3、M4 不标完成 |
| 儿童不理解题目或操作 | D3 样板反馈 | 先调整任务说明和交互模板，再批量生成其余活动 |
| 安卓声音、后台行为存在差异 | D1 初查、D8 全查 | 保留手动重播和陪读退路，使用缓冲修复；记录实测浏览器范围 |
| 在线托管账号未落实 | D8 | 局域网若可用则先交付本地体验；若局域网也不可用，将访问交付标为阻塞并列明所需条件 |
| 新增功能要求 | 每次范围变更时 | 区分替换与新增，更新任务、依赖和估算；不挤占核心流程及内容审校时间 |

优先保证学习闭环、声音、存储、内容正确和安卓可用。装饰动画精细度及额外过渡效果可压缩；首版约定的内容数量、核心页面和验收项目若需缩减，必须明确记录为范围变更。

### 13.5 执行跟踪与完成定义

开发开始后，在本节维护实际进度。状态统一使用“未开始／进行中／待验证／已完成／阻塞”；完成必须有运行结果、测试结果或真机记录支持。

| 里程碑 | 当前状态 | 实际完成日期 | 验证记录／阻塞 |
| --- | --- | --- | --- |
| M1 | 待验证 | — | 第一节课技术闭环、构建与单元测试已完成；热点地址电脑端返回 200；待安卓真机及试听素材审校 |
| M2 | 进行中 | — | 已实现十课解锁、独立进度、十故事书架、家长开放、30 字次日回顾；完整间隔调度和课前回顾仍待实现 |
| M3 | 待验证 | — | 十课、30 字、十张配画与整句音频已集成；正式素材与教学审校尚未完成 |
| M4 | 未开始 | — | 依赖功能、内容和真机验收 |

每完成一个里程碑，记录实际完成的任务编号、检查结果、尚未解决的问题及下一步。日程可以调整，但不能把“已写代码”等同于“用户可用”。

### 13.6 本次实现记录（2026-09-18）

- DEV-01、DEV-02：工程、移动布局和页面导航已实现。
- DEV-03：工作台会话 0 的 LearnBuddy 专用窗口托管服务，pane %7；电脑访问 http://10.127.206.238:5173/ 返回 HTTP 200，手机端待验证。
- DEV-04 至 DEV-07：第一节课数据、两类识别题、试听音频、共读和进度恢复已实现。声音属于待审听机器素材，M1 不标最终验收完成。
- DEV-08 至 DEV-12：已实现部分规则与配套页面；当前仅次日回顾，后续继续按 M2 范围完善。
- 当前标准：TypeScript 检查、生产构建、11 项单元／内容测试通过。3 条 Chrome 浏览器流程测试通过，详见 [验收记录](./验收记录.md)。
- 下一步：收集安卓样板反馈，修正交互，再补齐 M2 规则和其余课程内容。

### 13.7 统一执行计划（2026-09-21）

参见[开发计划](./开发计划.md)，该文件取代此前 DEV-18 至 DEV-25 与 ACC-01 至 ACC-04 的分散排期。路线采用推荐组合，任务状态不因本次计划更新而自动变为已实现。

新顺序为：ARC-01 数据基础 → ARC-02 前端接内容 API → ACC-01 登录与孩子档案 → ARC-03 内容管理与发布 → ACC-02 在线进度及错题 → ACC-03 离线同步及导入 → ARC-04／ACC-04 核心验收 → DEV-19 校准 → DEV-20／21 多图找字 → DEV-22／23 复习与纠错 → DEV-24／25 难度与成长。

DEV-18 保持“待验证（代码与自动化完成）”，不计入剩余开发工作量。ARC-01 已通过开发验证、待手机反馈，其余上述任务未开始；ARC-02 已进入接入验收，下一开发项为 ACC-01。计划明确了每个里程碑的用户体验、验收、数据迁移和外部资源依赖。

### 13.8 账号与学习数据规格

具体表模型、同步冲突、权限与旧数据限制见[账户与学习数据设计](./账户与学习数据设计.md)。每个孩子独立记录，核心写入由后端处理；后台认证基础与家庭账户复用实现但分离权限。工期与状态只在统一开发计划维护，避免与内容服务估算重复相加。

## 14. 最终交付清单

- 可运行的前端、后端、管理端源码、锁文件及构建产物；数据库迁移、内容导入与环境配置说明。
- 30 字、10 次活动、10 个一句话故事的内容文件与素材来源／审校清单。
- README：环境要求、安装、开发、测试、构建和热点访问步骤。
- 掌握及回顾规则测试结果、核心流程测试结果、安卓实机验收记录。
- 实际可访问的本地预览地址或在线链接，以及本机保存、浏览器范围等已知限制。
- 账号／档案隔离、服务端判题、错题追溯、离线同步及重复导入的验证记录。
- 数据库与素材备份恢复演练、发布回滚、家长导出删除说明。
- 更新后的统一开发计划、开发进度与未解决问题列表。

交付后再进行产品方案约定的两周家庭试用，收集操作求助、复习表现和亲子共读反馈，形成下一轮迭代任务；该阶段不计入上述开发排期。

## 15. 增量开发：场景找字（2026-09-18）

- 新增 `hunt` 活动类型及 `src/HiddenCharacters.tsx`，位于辨认练习与共读之间；当前活动由 11 步变为 12 步。
- 原创 SVG 场景结合真实文字按钮，按相对坐标将汉字放在风筝、木牌和花盆中；按钮尺寸 52 像素，支持手机点选与键盘访问。
- `huntFound` 保存找到的目标字，使用去重更新；提示、完成和亲子跳过都有明确反馈。重新开始活动会重置，继续活动与刷新保留。
- 内容版本升至 2；版本 1 的故事索引由 10 迁移到 11，其他有效位置与学习证据保持。旧存档补充空的 `huntFound`。
- 找字属于趣味巩固，不写入 `Attempt`，避免将视觉搜索表现当作独立认读证据。
- 验证覆盖点中、重复点击、提示、全部完成、跳过、刷新恢复、旧版本迁移和 320 像素布局。

### 一句话故事调整（2026-09-18）

课程结尾与书架统一呈现“爸爸妈妈和我一起看书。”，配套三个人围着一本打开的书阅读的原创插画，并播放整句音频。故事使用统一内容数据，避免课程与书架内容不一致；“和、一、起、看、书”为陪读字，家长辅助理解，不计入本课目标字。书架当前为一页完整图文故事。

## 16. 剩余章节扩展（2026-09-18）

- `src/content.ts` 改为十课数据与 `getSteps(lesson)`，各课统一生成十二步活动；30 字唯一，每课故事覆盖当课三个目标字。
- `src/LessonPicture.tsx` 提供剩余九幅图画；第一课复用已确认的亲子看书图。`HiddenCharacters` 按课程获取三个目标字，以课次轮换三个隐藏位置。
- `lessonProgress` 保存每课步骤、会话、找字与完成状态；`activeLesson` 标识当前课。切换前保存快照，重玩清空本次步骤及找字，但保留累计学习证据与历史完成状态。
- 内容版本升至 3；兼容版本 1、2 单课存档，不清空旧记录。逐课完成开放下一课，家长可以开放全课但不改完成统计。
- 书架按主题展示十个图画故事，地址使用 `#reader/<lessonId>`，刷新后仍展示对应故事。
- `scripts/audio-texts.json` 记录 30 个单字、10 个开场、10 个故事文本；生成脚本仅填充缺失试听音频。
- 范围调整：原三本主题绘本落实为三组主题书架下的十个一句话故事，保持用户确认的一课一图一句话模式。
- 后续保留：人工内容及语音审校、安卓实机逐章体验、完整间隔调度和课前回顾；不以内容集成完成代替这些验收。

## 17. 学习模式与找字优化实现规格（2026-09-21）

设计依据：[产品设计方案](./产品设计方案.md)第 3、6、11、12 节；官方调研来源统一保留在产品方案第 3.1 节，以下是本产品的实现选择。

### 17.1 独立读词、阶段导航与复习隔离（DEV-18）

- `Step.kind` 增加 `word`，每个目标字对应一个稳定步骤 ID `word-<characterId>`；十课统一在三张认字卡之后、辨认练习之前插入三个词语步骤。
- 显示“认字／读词语／玩一玩／读句子”阶段。词语页有标题、大词语、逐字高亮、“听词语”及陪读说明；不要求播放完才继续，也不采集录音。
- 已到达的读词、读句内容提供只读重练入口；家长开放课程也可预览。单独重读与正式活动推进分开，退出返回来源页面，不写入作答、完成、奖励或掌握记录。
- 初次学习依序推进，未到达内容的快捷入口保持不可用并说明需先完成前面的环节。已完成课程可从阅读页面重读词语。
- 内容版本升级时显式迁移旧步骤：版本 1 没有找字，版本 2、3 有找字但没有独立读词；当前活动及其他课程快照都要按原步骤语义映射，保留 session、已找到字、历史完成及学习证据。不能只迁移活动的顶层索引。
- 验证：十课全流程；刷新与切换课程；旧版本故事／找字／练习恢复；重读前后学习记录相同；直接链接不能绕过开放权限；320 像素阶段导航不溢出。

### 17.2 高亮、复习与证据（DEV-19、DEV-22 至 DEV-25）

- `readingTimings` 现为有声片段估算；DEV-19 需补充经审听的逐字时间点与校准状态，生成脚本不得覆盖已人工校准数据。保留单字、词语、故事统一播放器与错误处理。
- DEV-22 综合练习独立保存轮次、来源、本课关联、固定题单及题目版本、当前题序和退出/完成状态；复用现有作答与同步管线，服务端判题，刷新/换设备不重抽题，不把跨课候选写成正式课程步骤。字段与迁移在该项实现时确定。
- 回顾增加 `nextReviewDate`、`reviewStage`、`lastIndependentSuccessDate`；新接触未作答也能进入候选队列。当地日期加天计算，不直接用毫秒凑 24 小时；跳过／音频失败不按知识错误处理。
- 辅助练习记录原题关联和提示状态；延后纠错不覆盖首次回答，不制造同一会话的额外独立成功。原有作答去重规则必须覆盖新题型。
- 识字辨认、句意理解、跟读参与和家长观察分开记录；新模型要有迁移与持久化失败退路。找字及播放次数不计掌握。
- 难度参数集中配置；先根据明确的错误／提示规则调整，不新增外部 AI。成长摘要仅解释真实证据，休息、慢答和音频异常不降低知识评价。

### 17.3 场景与干扰字模型（DEV-20、DEV-21）

新增 `HuntScene`：`id`、`theme`、`assetVersion`、SVG 场景组件、文字安全位置 `slots[{id,x,y,clue}]`、中文描述与素材审校状态。位置使用相对坐标，触控区域保持至少 48 CSS 像素；底图不嵌入目标字或干扰字。

新增 `HuntRound`：`roundId`、`sceneId`、`sceneVersion`、`placements[{slotId,characterId,isTarget}]`、`foundIds`、`difficulty`。课程快照与当前活动均保存完整局面；随机选局只在新局创建时执行，渲染、重播和点击不消耗随机数。

- 每主题两幅，共六幅，清单见产品方案第 11.1 节。首次先做客厅与厨房样板，不复制花园只换色。
- 保存各主题最近场景 ID，新局从其余兼容场景选取；同课重玩至少改变一个目标字的位置，必要时确定性换位。保存最近布局签名供比较，不依赖概率保证变化。
- 目标固定为本课三字；默认首次 2 个、后续 3 个干扰项，家长下一局可选 2 或 4 个。先取已接触且非目标字，不足从审校候选池补齐；最终按显示汉字去重。每图至少 7 个安全位置。
- 目标和干扰字统一样式与语义按钮；点击干扰项只更新温和反馈，不调用 `onFind`，不播放失败警报，不写入 `seen`／`Attempt`。点击已找到目标不重复计数或重复播放。
- 提示按实际未找到目标的 slot 获取 clue，替换现有写死的风筝／木牌／花盆提示；背景点选使用场景通用提示。
- 旧存档进行中的找字局使用 legacy-garden 布局恢复，保留 `huntFound`，直到本局结束；新局才开始轮换。新存档的场景引用失效时根据版本映射恢复；不能悄悄换位并丢失已找到结果。
- 加载当前场景资源即可；SVG 沿用项目代码素材，无需新增图像生成依赖。素材清单记录每幅图来源和审核状态。

验收矩阵：同主题新局不连用一图；同课重玩换位；连续刷新、跨课切换、后台恢复保持本局；目标／干扰项不重复；每个目标恰好出现一次；提示仅指未找到目标；误点、重复点及提示不污染掌握证据；旧版找字进度保留；三主题全部场景在 320／393 像素与用户安卓浏览器中无点击重叠、裁切或资源缺失。


### 17.4 DEV-18 实现与交付记录（2026-09-21）

- 十课均增加独立读词阶段，每课从 12 步扩为 15 步；三个认字步骤后依次读三个词语。“读句子”使用明确标题。
- 新增 `ReadingPractice.tsx`：复用词语卡、阶段栏及只读重练页面。课程阶段栏可以重读已到达的词语／句子；书架故事增加“读词语”入口。原认字卡下的次要“读词语”播放按钮移至独立阶段，消除入口混淆。
- 只读重练使用独立 hash 路由 `#practice/<lessonId>/<words|story>/<lesson|reader>`，按课程进度、完成状态或家长开放权限校验；返回来源页面，学习步骤和掌握证据不变。直接打开无权限地址显示回小屋入口。
- 内容版本升至 4；旧版位置映射：v2／v3 的 0–3 保持，4–11 加 3；v1 的故事 10 映射到 14，其余练习位置相应后移。当前活动和其他课程快照一起迁移，保留找到的字、会话与答题记录；再次刷新不会二次迁移。
- 现阶段依然持久化数值步骤并显式按版本迁移；内容步骤自身有稳定 ID。今后统一改为持久化 stepId 时仍需兼容本轮存档，不能把本文目标模型误认为代码已经全部采用。
- 验证：生产构建、24 项单元／内容／音频测试、9 条 Chrome 浏览器测试通过，包含十课完整流程、旧版故事恢复、重读不改证据、未开放链接拦截和 320 像素布局。用户安卓实机体验待反馈。
- 本轮未完成项：逐字录音时间点人工校准（DEV-19）、六幅找字场景及干扰字（DEV-20／21）、完整间隔回顾等后续任务，状态见第 13.7 节。


## 多客户端实施约定（2026-09-21）

- Hono 路由仅处理输入校验、认证上下文和响应；内容发布、判题、进度及权益放在独立业务服务，数据库访问在后端。按 accounts/content/learning/entitlements 分模块，统一错误码、请求 ID、日志和运行时校验。
- 保持 `/api/v1` REST/JSON、OpenAPI 与共享契约。Node、数据库驱动和密钥不可进入客户端共享包；前端类型推断不能代替服务端校验。兼容未升级客户端，内容包声明版本及支持题型，兼容性测试覆盖旧契约。
- 网页与 API 通过反向代理同域；后续客户端直接使用 HTTPS API。网页 Cookie、移动端会话凭据由认证适配器转换为同一已验证账户上下文。管理员、孩子数据归属及商业权益分别检查；不把浏览器登录页作为所有客户端唯一认证入口。
- `/admin` 与学习端共用构建，按路由懒加载，独立页面布局。所有后台读写和发布接口服务端鉴权；公共课程接口不返回草稿。未来需要独立发布时再拆 `apps/admin`。
- 定义 AudioPlayer、LocalStore、NetworkStatus、AuthSession 等平台边界；网页分别使用浏览器音频、IndexedDB 和适合网页的网络/认证实现。播放时间、高亮、暂停及失败状态由播放器适配器提供；小程序/App 上重新验证，不假定浏览器能力通用。
- 同步协议使用稳定事件 ID、题目/内容版本与修订号；重试、并发及跨账户队列隔离必须测试。服务端决定判题与进度结果，共享纯规则只服务交互预览。
- 权益接口先实现免费策略，明确 accountId、learnerId、资源及动作。授权与额度的实际持久化、支付渠道和 webhook 在 PAY-01 实施，不提前接真实收费。
- Compose 保留数据库健康检查 → init 迁移与幂等导入 → API → web 的顺序，数据库和文件持久化；目标命令 `docker compose up -d --build`。Hono 切换后重新验证健康检查、退出时关闭连接、重复启动和备份恢复。

验证顺序：R1 验证数据库与部署；R2 验证内容 API 与十课回归；R3–R6 验证账户、权益边界与跨设备学习；R7 验证 HTTPS 和恢复；GROW-01 提供外网邀请试用。客户端选型、微信登录及支付规则在 CLIENT/PAY 阶段复核。不得把本地 HTTP 预览或接口脚手架作为公网试用、移动端适配已完成的证据。


## ARC-01 实施记录（2026-09-21）

已切换 Hono 4.13.8 / Node server 2.1.1，移除 NestJS、装饰器及其运行依赖。HTTP 层和内容查询分离，统一 JSON 错误、请求 ID、健康检查及 SIGTERM 连接关闭。现阶段只提供 `/api/health` 和 `/api/v1/content/inventory`，没有用户写接口。

现有内容导出为确定的 manifest，校验引用、音频时间点与素材 hash；存入课程/字/词/故事/步骤/题目/场景和素材表，同时保留发布快照。素材文件持久化，重复 seed 跳过已导入版本，拒绝同版本内容变化。基础账户、身份、会话、孩子、学习事件及作答表已建立，业务接通仍按 ACC 任务推进。

本机 Prisma 引擎未成功生成 SQL，改在 Linux 镜像中生成追加建表迁移；保留已记录的空迁移校验和，不删除已有库。新库迁移和模型一致性需持续检查。工具链的 deepmerge-ts/mysql2 修复版本通过 overrides 固定，当前安装审计为零漏洞。

部署采用 db → init → api → web 健康依赖。`/setup/` 可从数据库读取内容清单；网页的课程和进度目前仍是原型实现。维护与测试命令见[部署与维护](./部署与维护.md)。

## ARC-02 课程 API 实施规格（2026-09-21）

接口为 `GET /api/v1/catalog`、`GET /api/v1/catalog?releaseId=...`、`GET /api/v1/releases/:releaseId/lessons/:lessonId`，契约文档由 `GET /api/v1/openapi.json` 提供。目录只含主题、课程摘要、字索引、插画地址与步骤索引；单课包包含故事、编排、素材 URL、音频时间点及找字位置。内容从数据库读取，ETag 支持条件请求；无效版本/课程明确返回错误，不退回另一版本。

前端 `contentRepository.ts` 先校验目录，再按路由加载当前课；首次目录失败时不挂载学习应用，不写进度。单课失败保留当前存档并提供重试/回小屋。已加载课程在当前页面生命周期内可继续使用；此阶段没有持久化整课离线下载，断网刷新仍需联网重试，离线队列及同步由 ACC-03 实施。

本机存档新增 releaseId 与稳定 stepId。当前整个浏览器存档固定同一 release，课程/故事/重读均使用该版本；将来发布新版本后的显式升级与新旧课次并存由发布及账户阶段扩展，不能默认把未结束课次换版本。v1–v3 仍按旧步骤语义迁移，v4 数字游标首次保存时补稳定 ID；原始未绑定版本存档备份到 `learnbuddy:v1:progress:before-api`，备份失败时不覆盖原记录。未知内容版本保留原数据并提示恢复，不静默重置游标。

音频与插画来自 `/media/` URL，逐字时间点由内容包提供。生产网页不再复制原型 WAV，也不打包全量课程故事、插画 SVG 或阅读时间表；`apps/web/src/content.ts` 等旧数据/插画文件仅作为导入工具及测试来源。找字底图的导出源在 `scripts/import-content/PrototypeHunt.tsx`。网页 Vite 开发服务器代理 `/api` 和 `/media` 到本机 Docker 8080，因此开发学习页面也需要后端服务。

家长明确清除本机学习进度时，同时删除 `before-api` 迁移备份；浏览器拒绝删除时明确报错，不宣称清除成功。

## ACC-01 家长账户与孩子档案（2026-09-21）

认证使用 Better Auth 1.7.5 + Prisma adapter，挂载在 `/api/auth/*`。邮箱密码注册需验证邮箱；密码长度 10–128，找回后撤销全部旧会话。会话在 PostgreSQL 持久化，HttpOnly / SameSite Cookie，同域请求；生产启用 Secure。账号关联默认关闭，未来接微信／App 身份需显式验证现有账户后绑定，不按同名或未验证邮箱自动合并。

认证模型映射：Better Auth User → Prisma `FamilyAccount`（映射数据库 `Account` 表，避免与组件 account 模型重名），Account → `Identity`，Session → `AuthSession`，Verification → `Verification`。会话 token 由组件管理，原占位 `tokenHash` 改名为 `token`，不能声称数据库中存储的是摘要；数据库和备份按凭证保护。`Account.role` 默认 parent、禁止注册输入，内容后台须检查 admin。数字家长门仅是儿童误触防护，不是身份认证。

业务通过 `principal()` 获取已验证的账户身份。`GET /api/v1/me` 返回当前账户和孩子列表；`POST /api/v1/learners` 创建；`GET/PATCH /api/v1/learners/:id` 查询／改昵称。孩子只能属于当前账户，跨账户 ID 统一 404，禁止客户端提交 accountId；最多 8 个档案，并发创建通过账户行锁串行化。写操作验证精确 Origin。账户响应 no-store，不进入课程 ETag 缓存。

`GET /api/v1/learners/:id/entitlements` 是服务端免费权益策略入口，返回 capability、额度和到期时间；后续学习写入 API 需要调用同一策略，不依赖前端按钮隐藏。`GET /api/v1/admin/access` 验证管理员边界，当前尚未提供内容管理界面。

前端 `FamilyAccount.tsx` 封装认证请求与档案切换，儿童学习端从 context 获取存储键。游客保留 `learnbuddy:v1:progress`；孩子使用 `learnbuddy:v1:progress:<accountId>:<learnerId>`，切换时停止播放并重新挂载学习端。每个存档各自绑定内容版本、保留旧格式备份。账户／档案已存 DB；答题、错题与进度仍只在本机，ACC-02 接入云端，ACC-03 提供游客导入。不自动把游客记录归给首个孩子。

参考：[Better Auth Prisma](https://better-auth.com/docs/adapters/prisma)、[认证配置](https://better-auth.com/docs/reference/options)、[数据模型](https://better-auth.com/docs/concepts/database)。

## UX-01 手机学习单屏布局

`LearningActions.tsx` 将步骤操作通过 React portal 放入 App 的独立 footer 插槽，与可滚动 main 分开，避免 fixed 覆盖正文和每步滚到底部。`FamilyAccount` 提供布局外壳；学习路由使用 `100dvh`（`100vh` 兜底）的纵向 flex，包含账户提示、紧凑页头、内容和操作栏。内容 `min-height:0; overflow:auto`，操作栏不收缩并保留 safe-area-inset-bottom。切步、切课和内容就绪时重置内容区滚动位置。该改动不改变答题、音频高亮、课程 API 或存档格式。

验收使用浏览器内容视口 393×650、320×568，并动态缩短至 420px：逐一测量 15 个步骤的操作按钮位置／尺寸／横向溢出，以及阅读重练和完成页；另检查十课句子与陪读提示。较长内容仍可在中间滚动，不依赖隐藏浏览器地址栏或安装为 App。

## ARC-03 实现说明（2026-09-22）

- 新增 `ContentDraft`、`ContentChannel`、`ContentAudit`、`ContentUpload` 迁移。草稿允许暂时不一致的结构化编辑，发布使用更严格的跨引用、音频文字/时间点、场景位置与文件 hash 校验。
- seed 与后台复用 `release-writer.ts` 写入规范化发布内容；默认目录依据单一当前版本指针，显式 releaseId 始终可读历史版本。发布事务同时检查草稿 revision 和指针 revision，避免覆盖其他编辑者或管理员的操作。
- `/admin` 单独懒加载，复用成熟认证与服务端管理员校验；采用表单编辑、原生音频预览、逐字时间点编辑与场景预览。本轮不改变学习记录协议。
- 公共媒体入口改为 API 发布引用校验后提供内容，草稿素材在私有接口预览。支持 PNG 上传，旧 SVG 内容契约保留；音频仍为 WAV。
- 旧本机记录继续固定整个 release，不自动跳转到新版本。新建档案读取当前发布版本；按课并存和显式升级不冒充已经完成。


## ACC-02 在线学习记录（2026-09-22）

新增 learning-routes/service/rules：对孩子所属家庭鉴权、校验 Origin，并按孩子加事务行锁。课次启动 requestId 和事件 clientEventId 支持幂等重试；相同编号不同载荷冲突，题目呈现内只保留一份作答，陈旧修订返回 409。选项顺序、实际选择、提示、音频结果与跳过均入库，correct 由后端计算。课程累计完成与当前重玩位置分开，复习使用独立课次，重读不产生学习写入。

LearningSkill 和 MistakeItem 保存能力、次日队列与原始错题引用；规则版本为 1，服务端使用孩子时区（默认 Asia/Shanghai）判定学习日，提示/跳过/音频失败不计独立掌握，同日重复正确不直接升级为稳定。历史内容保留；当前按孩子整个档案绑定内容版本，尚不支持后续课次自动升级。

网页 CloudProvider 将云端快照投影为学习界面所需进度，保留原本机数据，只把声音及家长观察偏好写入独立本机键。写入期间和断网时冻结作答；响应丢失保留当前操作与编号供重试，刷新/聚焦读取服务端记录。听音题完整播放后才开放独立作答，切换孩子后忽略旧课程请求。离线持久化、旧存档导入及完整间隔调度留待 ACC-03/DEV-22。


## ACC-03 离线同步与旧存档导入（2026-09-22）

新增 sync-service、legacy-import，扩展共享 contracts/OpenAPI。服务端预登记题目呈现；按流序号缓存乱序提交，连续应用并逐项回执，失败事件使用保存点回滚但保留原始提交。复用孩子事务锁、修订号和答案去重；冲突不自动变基或覆盖。版本化摘要支持按 revision 的字段级增量和被替换课次的移除标记。未知基线全量恢复；没有实现账号/档案删除接口。

网页 offlineStore 提供 IndexedDB 事务与 generation 比较写入；offlineLearning 生成独立的本机预览，不能修改已确认快照或凭离线反馈增加掌握。CloudProvider 落盘后发送批次，仅 applied 清除待同步项；切账号隔离、原账户重登补传、冲突副本保留。课程和素材按版本缓存；Vite 输出 Service Worker 缓存应用外壳，不缓存私有 API。HTTP 局域网无 Service Worker 时仍支持当前页面离线与联网重开后的队列恢复。

导入经家长中心预览和选择目标确认，服务端校验旧格式及版本、指纹去重、保留 legacy_import。已完成取并集、云端已有位置优先；旧记录不制造实际选项、错题快照或新掌握证据。规则版本 2 对迟到或时钟异常作答保留事实，采用保守的时间可信标记，不据此制造跨日成功。详细行为和当前限制以《账户与学习数据设计》ACC-03 实现协议为准。
