# LearnBuddy 本地部署与维护

本地运行组合为 Hono / Node.js、PostgreSQL 17、Nginx 与持久化素材目录。网页通过 API 加载课程与素材；已接入邮箱账户、孩子档案、内容后台、云端学习与离线补传。R7 本地版提供家庭数据导出/删除及防止恢复已删除数据的凭据重放。支付尚未开发。

## 一键启动

安装并启动 Docker（含 Compose v2），在项目根目录执行：

```sh
docker compose up -d --build
```

初次需联网下载镜像及依赖。默认浏览器地址为 http://localhost:8080/，数据库验收页为 http://localhost:8080/setup/。手机与电脑网络互通时使用 `http://电脑局域网IP:8080/setup/`；这是本地试用入口，不是公网发布地址。

仅对外映射 web 的 8080 端口，数据库和 API 只在 Compose 网络访问。未创建任何默认管理员账户。本地数据库有开发默认口令；部署前复制 `.env.example` 为 `.env` 并设置独立口令及需要的 WEB_PORT。当前口令嵌入连接 URL，应使用足够长的 URL 安全随机字符。已有数据库更改环境变量不会自动修改数据库内口令。

启动依赖：数据库健康 → init 执行已提交 SQL 迁移和内容校验/导入 → API 健康 → web。init 正常退出 0 是预期状态。诊断：

```sh
docker compose ps -a
docker compose logs --tail=100 init api
curl http://localhost:8080/api/health
curl http://localhost:8080/api/v1/content/inventory
```

`/setup/` 用于查看数据库清单；学习页面使用下文 ARC-02 的目录与单课 API。8080 与旧 5173 地址的浏览器存档互不共享；不要清除旧记录；家长中心已提供旧存档预览及确认导入。

## 内容和迁移

原型导出命令为 `npm run content:export`，输出到忽略版本控制的 `content/`。镜像构建自动执行导出。素材按 SHA-256 命名，文件存于 `media_data` 卷，元信息、文字及版本存于数据库。源数据当前来自 `apps/web/src/content.ts`，编辑内容使用 `/admin` 的草稿、校验和发布流程；原型导入只作初始种子。

重复初始化校验全部文件；同 releaseId、同内容校验和跳过写入，不覆盖数据库已有内容。源内容变化但 releaseId 不变会拒绝导入，需按发布/版本流程处理。数据库/文件错误不得用清库或换成另一批内容来掩盖。

迁移 `202609210001_foundation` 是早期本机引擎留下且已记录的空迁移；保留原校验和，`202609210002_create_tables` 追加真实建表 SQL。新库连续执行这两步即可。开发环境的迁移引擎若不能运行，可用相同 API 镜像在 Linux 容器内生成 diff；不得提交空 SQL 作为建表完成的证据。

## 持久化与备份

`docker compose stop` 停止服务；`docker compose up -d` 恢复。`docker compose down` 删除容器和网络但保留命名卷。不要添加 `-v`，它会删除数据库、素材、认证密钥和删除凭据卷。

```sh
sh scripts/backup.sh .backups/manual-check
sh scripts/verify-restore.sh .backups/manual-check
```

备份包含 `database.dump`、`media.tar.gz` 和 `deletions.tar.gz`；每次使用独立路径。恢复验证只操作临时数据库和临时素材目录：先追加当前迁移，再重放备份内及当前外部删除凭据、撤销恢复库全部旧会话，随后核对课程/素材 hash、学习事实和删除目标不存在。结束后清理，不覆盖运行库或撤销当前服务的会话。正式灾难恢复必须遵循下方 R7 流程后再开放流量。

当前先做 DB 一致性快照，再打包只追加的已发布/暂存素材；晚于快照的孤立文件允许存在。引入素材删除/GC 时必须增加快照锁或暂停写入。当前备份保存在本机，公网部署时另行配置离机备份、保留期限和恢复权限。

## 开发验证

```sh
npm ci
npm run build
npm test
sh scripts/test-database.sh
PLAYWRIGHT_BASE_URL=http://127.0.0.1:8080 npm run test:e2e
```

数据库测试在临时库迁移和导入，测试重复导入、不覆盖内容、归属约束及 HTTP 接口，结束后删临时库。不要直接对有用户数据的库运行 API 工作区的 integration 测试。浏览器测试使用已运行的服务；需要本机 Chrome。

工作台中的常驻开发服务和 Compose 构建任务通过 `tmux-agent-workbench run` 托管，遵循项目工作台约定。正式公网试用还需要 HTTPS、登录与权限、适用素材审校和外网手机验收，见统一开发计划 GROW-01。

## 构建网络

若电脑能联网但容器下载 npm 包超时，可在不提交版本控制的 `.env` 中设置 `DOCKER_BUILD_HTTP_PROXY` / `DOCKER_BUILD_HTTPS_PROXY`，指向容器可访问的现有代理（Docker Desktop/OrbStack 可使用 `host.docker.internal:端口`）。随后仍执行同一 Compose 启动命令。代理通过 Docker 预定义构建参数传递，运行服务不继承这些变量；不要关闭 TLS 校验。没有代理需求时留空即可。

## 课程 API（ARC-02）

学习页面现在先请求 `/api/v1/catalog`，进入活动/重读时再请求 `/api/v1/releases/:releaseId/lessons/:lessonId`；素材从 `/media/` 读取。OpenAPI 文档位于 `/api/v1/openapi.json`。目录支持指定 releaseId；课程请求和本机进度绑定同一版本，避免内容更新影响在学位置。

旧存档在第一次保存新格式前备份到浏览器的 `learnbuddy:v1:progress:before-api`。家长明确清除本机进度时同时删除这份迁移备份。备份仍在原浏览器和原网址，不是服务器备份；学习记录尚未云同步。API 失败时显示重试并保留存档，已加载的课程可在当前页面继续；断网刷新不保证可用，完整离线缓存是后续任务。

## ACC-01 账户服务配置与本地邮件

开发环境仍可 `docker compose up -d --build --wait` 一键启动，新增 Mailpit 本地邮件接收器。默认 `AUTH_MODE=local`，启动时在 `auth_data` 卷生成随机认证密钥并持久保存；重启不更换密钥。默认只信任 localhost/127.0.0.1 的 8080 端口。手机验收时在 `.env` 的 `AUTH_ORIGINS` 追加**完整手机访问网址的 origin**（例如 `http://192.168.1.10:8080`），修改后重新创建 api。Vite 5173 也需明确加入。

注册、重发验证和密码找回邮件只进入 [本机测试收件箱](http://localhost:8025)，不会发往真实邮箱。收件箱仅绑定电脑 127.0.0.1；手机注册后可在电脑打开收件箱，将邮件中的验证链接复制到同一手机浏览器打开。建议使用 `xxx@learnbuddy.test`，勿使用真实密码。邮箱地址代表测试身份，并不证明真实邮件所有权。

正式发布前必须设置 `AUTH_MODE=production`、精确 HTTPS `AUTH_ORIGINS`、32 字符以上随机 `AUTH_SECRET`、真实 SMTP_HOST／PORT／USER／PASSWORD／MAIL_FROM；非 TLS 直连使用 STARTTLS（requireTLS），或 SMTP_SECURE=true 使用隐式 TLS。缺少生产密钥／SMTP 或使用 HTTP origin 时拒绝启动。生产不需要启动 mailpit，可只启动 db/init/api/web。当前局域网 HTTP 和 Mailpit **不算生产认证验收通过**。

Nginx 保留包含端口的 Host，覆盖 X-Real-IP，API 不向主机暴露端口；不要绕过此代理将 API 直接开放公网。Auth 限流从可信代理 X-Real-IP 识别来源；生产在其他负载均衡后部署时需重新审核代理信任链。

数据库备份包含邮箱、密码哈希、会话凭证和验证数据，须限制访问。`auth_data` 或生产 AUTH_SECRET 需另行安全保管；遗失会使旧会话和验证链接失效，可重新登录／发送邮件恢复，切勿把密钥提交进仓库。迁移 `202609210003_family_auth` 保留原账户／学习记录，为旧占位账户分配 `.invalid` 邮箱并使旧占位会话过期。

## ARC-03 内容后台（2026-09-22）

后台入口为同域 `/admin`，与学习端共用前端工程、按路由懒加载。先在 `/#account` 登录已验证邮箱。普通家长不能读写草稿、上传文件、预览私有素材或发布。管理员由部署维护者明确指定，注册参数不能自授予：

```sh
docker compose exec -T api node dist/grant-admin.js '<已注册并验证的家长邮箱>'
```

登录后的管理员也可从家长账户页点击“打开内容工作台”。邮箱验证与 SMTP 设置仍沿用原配置。

体验步骤：

1. 从当前版本“新建草稿”，修改课程名称、词语、例句、故事或陪读提示，点击“保存草稿”。
2. 修改开场、词语或故事录音文字时，在“素材与声音”选择对应素材，上传新的 PCM WAV，填写一致的录音文字和逐字起点；图片替换使用 PNG。上传支持最大 20 MB，音频最长 3 分钟，PNG 最大 4096×4096。已导入的项目 SVG 继续可用，本轮不开放 SVG 上传。
3. 点击“校验并预览”，核对步骤、文字、配图、找字位置、声音与逐字高亮。校验不代替人工审听、教学审校和授权确认。
4. 点击“发布为当前版本”。内容按新 releaseId 写入原有课程、字、词、故事、步骤、题目、场景与素材表，事务切换当前版本；发布快照不再修改。失败不会切换目录，重复发布或修订冲突返回 409。
5. 在“版本历史与回滚”启用旧版本。新档案使用当前版本；已有本机存档仍固定原 releaseId，可继续学习。现阶段整个档案固定同一版本，不自动升级或混合新旧课次；按课版本并存由后续学习记录阶段扩展。不要以清除已有学习进度作为体验新版本的步骤，可使用新的孩子档案或独立浏览器。

技术边界：草稿独立保存为带 revision 的编辑文档，正式发布仍写入规范化内容表；多个编辑者使用修订号防止覆盖。原型课程/主题/步骤/汉字稳定 ID 保持不变；本轮编辑已有十课，不包含新增课型和六幅新找字图。所有素材仍标记待人工审校，发布仅用于内部体验。

上传原件位于素材卷 `.drafts/assets/...`，通过管理员 API 私有预览。公共 `/media` 由 API 检查素材已被正式版本引用，支持 WAV Range 和不可变缓存；Nginx 不直接暴露整个素材卷。发布前的拷贝或失败发布留下的文件不会通过公共 API 泄露。文件只增不删，暂不实现清理。

备份先取得数据库快照，再归档完整素材卷（含 `.drafts`）；只增不删保证快照中引用的文件仍存在，多出的孤立文件不影响恢复。未来若增加素材清理，必须增加备份互斥。恢复验证还检查当前版本指针及上传文件 hash；旧版备份需先执行迁移才能使用新版恢复检查。

接口索引见 `/api/v1/openapi.json`。后台接口位于 `/api/v1/admin`：`content`、`drafts`、`drafts/:id`、`drafts/:id/validate`、`uploads`、`media/:hash`、`releases`、`releases/:id/activate`。所有修改需要可信 Origin；返回 `Cache-Control: no-store`。管理员授予、草稿创建/保存、发布与切换版本均记录审计事件。

本次实际验证的工作台为会话 `0` 的 LearnBuddy 检查窗口（2026-09-22）；构建/恢复 pane `%9`、测试 pane `%8`。后续仍须重新核实会话和 pane 归属，不能直接假定这些编号永久有效。

## ACC-02 在线学习（2026-09-22）

学习入口不变。家长登录并选择孩子后，新学习通过 `/api/v1/learners/:id/` 在线保存；游客仍保存在本机。

建议体验：登录并选择孩子 → 开始课程、推进几步 → 在另一浏览器登录同一账户、选择同一孩子并继续 → 做一次独立答错 → 家长中心查看实际所选答案和原题选项 → 返回读词重练，确认正式位置保持。每个孩子独立保存。家长开放课程只修改访问设置，不增加已完成数量。

已有浏览器旧记录不自动导入，也不清除；选中旧档案时会提示原本机记录保留。新学习与旧本机存档并存，若要将历史进度合并到云端，等待 ACC-03 的明确导入流程。云端学习需要联网：未确认的操作可在当前页面“重试保存”，409 时读取最新进度；还没有跨刷新持久化待同步队列。完整离线学习、乱序补传和迁移不要标记为已完成。

后端新增 `202609220002_online_learning` 迁移；备份脚本已覆盖这些表，恢复检查新增课次版本/孩子归属、实际答案判定、错题引用检查。有云端记录后，不应将 API 回退到只支持本机记录的旧版。

## ACC-03 离线队列与旧记录导入（2026-09-22）

迁移 `202609220003_offline_sync` 新增同步流/逐条回执、进度摘要版本和导入凭据表，Attempt 增加时间可信标记。部署前备份 `.backups/pre-acc03-sync-20260922`；迁移追加新表列，不清空学习数据。

体验步骤：登录并选择孩子 → 联网开始一课、加载课程 → 在已打开页面断网继续 → 确认“待同步 N 条 · 已保存在本机” → 联网后确认云端保存。新课次需联网。原账户重新登录才能补传其队列；同步冲突时原始提交保留，点击“保留本机副本，继续云端位置”后按云端继续，不自动覆盖。

家长中心 → 展开“本机旧记录已保留 · 导入记录” → 选来源和目标孩子 → 查看摘要 → 确认导入。目标孩子有待同步项时需先处理；版本不兼容时可选择尚未开始学习的新档案。原存档不删除，重复导入不多记；导入凭据可从 `GET /api/v1/learners/:id/imports` 查询。当前导入不恢复旧版缺失的实际选项，不把布尔正确当作新掌握证据。

IndexedDB 数据可能因清理站点数据/浏览器空间回收丢失；页面仅在写入成功后显示本机保存。部署或排错不要清除仍有待同步记录的站点存储。确认云端保存后才有服务端备份保障。缓存应用外壳需要 HTTPS 或 localhost，现有手机 HTTP 局域网地址只能在已打开页面离线继续；断网关闭后重新打开网站不作为该地址的承诺。浏览器更新外壳在旧标签关闭后激活，资源按构建版本分开缓存。

本轮最终备份 `.backups/acc03-sync-verified` 已在独立库与素材目录恢复通过，含同步流、导入凭据和进度摘要快照；具体结果见《验收记录》ACC-03。

## R7 数据管理与删除后的恢复（2026-09-22）

用户已确认暂无外网环境，本轮仅验证本地服务。入口为 `/#account` →「导出与删除数据」。删除请求需可信 Origin、已验证的本人会话、当前密码及精确昵称/邮箱，服务端每个账户每五分钟最多验证五次。删除操作请求编号可以重试；账户已删除后会话失效，可通过本机清理凭据确认结果。

数据删除覆盖孩子档案、原始事件/实际作答、题目呈现、课次、累计完成/能力/错题、同步流与事件、导入及增量摘要；账户删除额外清除身份密码、全部会话和重设密码记录。公共教学内容、共用素材和内容审计保留，作者关联置空，不删除供其他家庭使用的版本。浏览器清除对应账户/孩子的学习缓存、待同步队列、冲突副本及旧档案偏好；独立游客存档和家长主动下载的文件不受影响。

### 删除凭据不可随旧数据库回滚

- 新增 `deletion_data` 卷，挂载为 `/app/deletions`（`DELETION_LOG_DIR`）。删除前先将最小凭据写盘并 fsync，再提交数据库事务；文件只含请求/账户/孩子 UUID、时间、范围及清理凭据散列，不保存邮箱或学习内容。API 启动先重放全部凭据，失败不对外服务。
- 删除授权一旦持久化，数据库提交中断也会在重试或重启时完成。因此页面断线不代表删除取消；重新连接检查档案或用原请求编号重试。
- 日常备份包含当时的凭据副本，但恢复旧备份仍必须拿到**删除发生之后的最新凭据**。禁止仅恢复旧 DB 和其旧凭据后开放服务；禁止删除、清空或用旧卷覆盖最新 `deletion_data`。本地环境尚无异地冗余，丢失全部最新凭据后不能保证旧备份不会恢复已删除数据。
- 备份内旧记录并非立即物理擦除，而是在恢复时按凭据移除，备份仍需受限访问。正式部署前要确定备份保留/到期删除及凭据独立持久化策略；不能在仍有可恢复旧备份时清除相应凭据。

### 隔离恢复与正式切换顺序

`sh scripts/verify-restore.sh BACKUP_DIR` 自动在临时库进行：恢复 dump → 追加当前迁移 → 重放备份内凭据 → 重放当前最新凭据 → 撤销恢复库所有会话 → 验证学习关系、各版本和素材哈希、删除目标不存在。支持升级前没有 `deletions.tar.gz` 的备份，但仍使用当前最新凭据；原服务不受影响。

正式恢复必须先停止目标入口与所有写入方，在隔离环境按同样顺序完成上述步骤，保留最新凭据卷并检查数据库/代码迁移兼容，确认后才切换流量，家长重新登录。不要直接用旧数据库替换正在提供服务的库，也不要降级到不认识删除凭据的 API。恢复目录和备份都只接收可信运维文件。

### 本地自动化与正式验收的边界

本地集成测试覆盖生产配置拒绝 HTTP Origin、Secure/HttpOnly/SameSite Cookie 和 HTTPS 验证链接生成；测试邮件由 Mailpit/注入回调接收。这不等于验证真实域名证书、SMTP 投递或手机 Cookie/音频表现。真实 HTTPS 手机登录、实际安卓双设备续学与邮件链路继续保留待验证。

已加载新版并保存清理凭据的设备，在启动、回到前台、联网或周期检查时核对删除状态。普通会话过期/退出保留待同步记录，只有可信删除结果才清理。长时间离线设备无法立即远程擦除；从未加载新版、未保存清理凭据的旧浏览器需在该设备上手动清理缓存，但旧请求仍会被服务端拒绝，不能恢复被删除的学习数据。


## 1000 字课程本地发布（2026-09-22）

完整版本 `curriculum-1000-v1`：204 课／1000 字／2632 条素材引用。前十课、其 ID 和素材原样保留，追加 194 课；每课允许 3—5 字。完整包保存在 `curriculum-release/`，不打入原型 seed；容器素材已经复制进持久卷，后续重建容器不丢失课程。

操作流程及命令见 [课程发布手册](scripts/curriculum/README.md)。`publish-curriculum` 是授权运维入口，核实指定邮箱已验证且为管理员，再校验媒体文件并事务写入新版本、切换指针和审计。后台保存上限扩为 16 MiB，发布事务上限 180 秒。原型导入仍然只做幂等初始化，不会把当前指针覆盖回十课。

新增 `GET /api/v1/catalog-upgrade?from=版本` 查询可兼容的新目录；`POST /api/v1/learners/:id/curriculum-upgrade` 在归属与 Origin 校验后，仅允许旧课程、主题、素材全部相同且追加新课的版本升级。旧课次与题目编号继续保留。当前版本指针回滚不会删除已经绑定新版的记录。

`GET /api/v1/learners/:id/reviews?lessonId=已完成课ID` 返回课末综合练习：本课至多 2 个字、历史错题或到期内容至多 3 个字，同字去重，不把未学字填入历史题。无参数仍为既有到期复习队列。

发布前备份：`.backups/pre-curriculum-1000`。正式 HTTPS／域名仍未配置，本轮发布范围为本机和网络互通的手机。

发布后完整备份 `.backups/curriculum-1000-published` 已恢复验证通过。恢复检查允许档案目录相对旧课次为严格追加扩容，仍拒绝任意版本不一致；新旧课次、题目引用及删除凭据均会核验。最终浏览器窄屏复验通过，详见《验收记录》“完整课程最终补记”。
