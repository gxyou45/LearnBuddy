# LearnBuddy 账户、学习进度与错题设计

更新：2026-09-22 ｜ 状态：云端账户、在线学习记录、已缓存课次离线同步及旧存档确认导入已接通；真实手机验收待完成

执行计划已整合到[开发计划](./开发计划.md)，采用推荐技术路线。本文保留设计与初步分项估算，实际顺序、剩余工期和状态以统一计划为准，不重复累计。

## 1. 推荐结论

采用 Hono on Node.js + Prisma + PostgreSQL。教学内容与用户学习数据使用同一 PostgreSQL 服务中的不同表／schema，由后端模块分别管理；第一阶段无需新增 MongoDB、Redis 或事件消息队列。图片音频继续使用对象存储。

采用“家长账户 → 孩子档案 → 学习记录”的归属关系。一个家长可管理多个孩子，所有学习记录按 learnerId 隔离，不把兄弟姐妹的进度合成一份。默认创建一个孩子档案，孩子不需要手机号、邮箱或密码。首期一个档案归属一个家长账户，多监护人共享可后续引入授权成员关系。

**每次作答保留事实记录，课程进度、薄弱项与复习计划由后端汇总。** 原始记录不因下一次答对而覆盖；显示错题时结合当时题目版本和实际所选答案。

## 2. 技术选型

| 层次 | 推荐 | 原因与替代 |
| --- | --- | --- |
| 身份与登录 | Hono 接入成熟认证组件，使用服务端会话 | 首期网页同域代理适合 HttpOnly Cookie；认证代码、找回与密码处理使用成熟实现，不能照搬演示代码上线 |
| 登录方式 | 首期邮箱＋密码、邮箱验证与找回；手机号验证码可替换 | 明确接口隔离，登录渠道不影响 learnerId；若正式用户更需要手机登录，可接入短信服务后切换，不先要求孩子提供联系方式 |
| 会话保存 | PostgreSQL 会话存储 | 初期与业务库共用服务但独立表；可撤销设备会话。Redis 后续有实际负载需求再加入 |
| 学习数据 | PostgreSQL + Prisma | 用外键保证归属，用唯一约束去重，用事务保持作答、进度及错题一致 |
| 本地缓存／待同步队列 | IndexedDB | 保存已缓存课程与待发送事件；localStorage 只留轻量偏好及旧版迁移入口 |
| API | REST + OpenAPI | 学习事件批量提交、进度读取、错题及复习查询；不开放浏览器直接写核心业务表 |

认证功能采用成熟组件，不自行发明密码或令牌协议；Hono 中间件统一解析会话、检查权限并传递已验证身份。网页使用持久化服务端会话，不能使用进程内存作为生产会话存储。具体认证组件在 ACC-01 实施前核验对 Cookie 与后续移动客户端的支持。

浏览器使用 Secure、HttpOnly、SameSite Cookie，服务端校验写请求来源并防护 CSRF；登录后轮换会话 ID，支持退出及撤销其他设备。手机正式验证使用 HTTPS；现有 HTTP 局域网链接不能直接验证生产 Secure Cookie，需配置 HTTPS 测试入口或独立的开发配置，不能放宽生产配置迁就本地测试。

替代方案是 Supabase Auth + PostgreSQL：它可减少认证基础功能的建设，但不能替代学习证据与冲突合并逻辑。若浏览器直接查询其数据 API，必须配置行级策略；核心作答仍建议由后端判题并写入，服务角色密钥仅存在后端。[Supabase Auth](https://supabase.com/docs/guides/auth)、[行级安全](https://supabase.com/docs/guides/database/postgres/row-level-security)

Payload／Directus 可以继续管理教学内容，儿童账户和学习记录不应当作 CMS 编辑人员账号，也不通过 CMS 通用后台直接修改学习结果。无论是否采用 CMS，学习业务仍有独立 API 边界。

## 3. 数据模型

所有主键使用稳定 ID；学习记录通过外键关联档案、课次和题目版本。多个孩子共用内容，不为每个孩子复制一套课程。

| 表 | 核心字段 | 职责与唯一性 |
| --- | --- | --- |
| accounts | id、状态、创建时间 | 家长主体，不直接保存学习游标 |
| account_identities | accountId、provider、providerSubject | 登录方式绑定；provider＋providerSubject 唯一 |
| auth_sessions | id、accountId、设备说明、到期／撤销时间 | 登录会话，与学习课次分开 |
| learners | id、ownerAccountId、昵称、头像、学习时区 | 孩子档案；无需真实姓名、完整生日 |
| learning_sessions | id、learnerId、lessonId、releaseId、mode、开始／结束时间 | 一次学习或重读，mode 区分正式学习、复习、只读重练 |
| question_presentations | id、learningSessionId、stepId、questionVersionId、选项顺序、变式配置 | 记录实际展示的题目实例，重放错题时不会套用新版题目 |
| learning_events | id、learnerId、sessionId、clientEventId、deviceId、clientSeq、类型、payload、客户端时间、接收时间、来源 | 作答、提示、步骤完成等追加事实；learnerId＋clientEventId 唯一 |
| attempts | eventId、presentationId、所选答案、后端判定、提示／跳过／播放异常、作答序号 | 结构化作答；eventId 唯一，同一呈现与作答序号唯一；辅助重试与首次回答分开 |
| learner_lesson_progress | learnerId、lessonId、首次完成时间、最近学习时间 | 课程累计状态；learnerId＋lessonId 唯一，重玩不取消历史完成 |
| session_progress | sessionId、currentStepId、completedStepIds、revision | 本次步骤与恢复位置；sessionId 唯一，使用版本号处理并发 |
| learner_skill_state | learnerId、targetId、skillType、状态、证据摘要、ruleVersion | 按字／能力维护辨认情况；learnerId＋targetId＋skillType 唯一 |
| mistake_items | learnerId、questionVersionId、targetId、skillType、首次／最近错时间、次数、状态 | 错题聚合索引；保留到原 attempts 的可追溯关联，不是原始记录的替代品 |
| review_schedule | learnerId、targetId、skillType、stage、dueDate、最近有效日期、ruleVersion | 按学习目标安排复习，避免一道错题产生多条重复到期任务 |
| hunt_rounds | id、sessionId、场景版本、placements、foundIds、revision | 保存找字局面；切设备或刷新保持原图、位置和发现结果 |
| parent_observations | learnerId、targetId、结果、记录时间、accountId | 家长认读观察，单独呈现，不混入自动题目证据 |
| legacy_imports | learnerId、sourceId、导入指纹、状态、统计 | 本机旧记录导入去重与追溯 |

首期可将 learning_events 与 attempts 作为同一写入事务中的通用事件表和结构化子表；若实现成本需压缩，也可先统一用一张带类型的记录表加视图，仍须保留索引、题目版本及唯一性。无需上完整事件溯源平台。

建议索引：attempts 的档案／时间查询可经 session 关联，热点查询可冗余 learnerId 并用组合外键保证同属一个档案；mistake_items(learnerId,status,lastWrongAt)、review_schedule(learnerId,dueDate)、learning_events(learnerId,serverRevision)。事件、档案、session 的归属不能只由客户端声明。

PostgreSQL 的唯一约束、外键和事务是上述一致性的基础；约束不代替业务权限校验。[约束](https://www.postgresql.org/docs/current/ddl-constraints.html)、[事务隔离](https://www.postgresql.org/docs/current/transaction-iso.html)

## 4. 学习进度的含义

分开保存三件事：

1. 课程完成：已完成过哪一课，解锁哪些内容。
2. 当前学习位置：哪个课次、哪一内容版本、哪个稳定 stepId，以及找字局面。
3. 能力证据：某字在听音辨认、看字理解等任务中的表现，以及到期复习计划。

播放词语、读句子、找字成功或点击继续只更新相应活动进度，不自动记为独立识字成功。只读重练不改变正式课程的恢复位置。家庭成员或不同设备可以重玩已完成课程，历史完成状态仍保留。

## 5. 错题记录与薄弱项

例子：系统读“爸”，孩子选了“妈”。记录题目版本、当时选项顺序、选择“妈”、正确答案“爸”、是否提示、所属孩子与课次、时间及事件 ID。服务端根据已保存的题目版本判定结果，不接受客户端传来的 correct=true 作为事实。

- 首次独立答错：记入原始作答，更新错题次数，并将“爸／听音辨认”列为需复习项。
- 看提示后答对：保存为辅助完成，不清除首次错误，不提升为独立掌握。
- 跳过或音频失败：保留参与记录和原因，不按知识错误累计；没有有效音频的听音题不应贡献独立成功。
- 找字点到干扰项：只做当前游戏反馈，不进错题本，不把干扰字标为已学。
- 下一次独立答对：保留历史错误，进入“待巩固”；达到跨日、不同任务的证据规则后变为“近期较稳定”。不凭一次纠正就永远移除。
- 再次独立答错：回到待练状态，缩短复习间隔；不影响已经完成课程的历史记录。

错题本按题目显示“曾经错在哪里”，复习队列按 targetId＋skillType 选择要巩固的能力，可以换题或换场景；练过原题不等于只记答案就算会了。孩子界面使用“再认一认”，家长页可查看具体历史。旧题下架仍保留可追溯的版本信息；内容纠错导致原判定失效时标记该证据无效并重算汇总，不静默改历史。

首期沿用产品方案的跨日至少两天、不同任务类型等启发式规则，保存 ruleVersion；读句理解和字音辨认分别解释，不合成不透明分数。

## 6. 作答写入与多设备同步

首期采用“云端权威状态＋本机待同步队列”。在线时每题提交，前端可立即给练习反馈，但云端重新判定与计算；断网时对已缓存、版本明确的题目暂存事件，不宣称已经保存到云端。首期离线仅继续已建立的学习课次；新建课次或加载未缓存课程需要联网，避免将未登记的题目实例直接当成可信记录。

写入过程：验证登录与档案归属 → 校验 session、题目版本及事件格式 → 检查事件去重 → 写入原始作答 → 更新课程／错题／复习汇总 → 同一事务提交 → 返回确认与最新 revision。事务失败整体回滚；重复发送同一事件返回已处理结果。相同 ID 携带不同内容则返回冲突，不静默覆盖。

| 冲突情形 | 处理方式 |
| --- | --- |
| 网络重试、同批重复发送 | clientEventId 唯一约束；同一题实例的首次提交另做语义去重 |
| 同一设备离线补传乱序 | 每 session 带 clientSeq；已接收与已应用分开确认，缺序事件等待补齐／明确修复，不将后到的提示当成独立答题 |
| 两台设备做不同课次 | 原始事件均保留，分别恢复各自 session；课程完成取已完成事实，不用整份 JSON 互相覆盖 |
| 两台设备同时推进同一课次 | 提交 expectedRevision；服务端串行更新或行锁校验，旧游标写入返回 409；返回可选的继续位置，不以最大步骤下标粗暴合并 |
| 离线事件晚于其他设备到达 | 作答事实按约定顺序重新汇总受影响目标；旧 completedStep 不撤销新完成，旧错误不直接覆盖最新能力快照 |
| 切换账户或孩子 | 队列按原 accountId／learnerId 隔离；未同步项保留给原账号，重新登录后再发，不能归给当前新账号 |

记录客户端发生时间、客户端时区及服务端接收时间。复习使用档案设定的学习时区；来自同一 session 的次序按 clientSeq。跨设备迟到记录按经校验的发生时间及稳定 ID 重算，设备时间明显异常或无法确认日期的离线记录保留但不用于制造新的跨日掌握证据。界面需允许“待同步”“已同步”“需要重新登录”。

第一版不必使用 WebSocket；进入页面、切回前台和提交后拉取最新状态即可。同步接口使用每个档案的递增修订号或服务端变更游标，不仅依赖 updatedAt，避免同时间戳漏记录。新设备首次获取完整摘要，此后增量；记录删除需要同步删除标记。

IndexedDB 适合保存结构化本地数据及队列，但浏览器存储仍可能被清理；仅服务端确认的数据可称为已云端保存。[IndexedDB](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API)

## 7. API 草案

| API | 用途 |
| --- | --- |
| `POST /api/v1/auth/login`、`POST /auth/logout` | 家长登录／退出，设置或撤销会话；验证码／找回按选定认证组件提供 |
| `GET /api/v1/me` | 返回当前账户与可访问的孩子档案 |
| `POST /api/v1/learners` | 创建孩子档案 |
| `POST /api/v1/learners/:id/sessions` | 创建固定内容版本的学习课次 |
| `POST /api/v1/learners/:id/events:batch` | 提交作答／步骤等事件，返回逐条接收与应用结果及最新修订号 |
| `GET /api/v1/learners/:id/progress` | 课程完成、各课次继续位置和能力摘要 |
| `GET /api/v1/learners/:id/mistakes?status=active&cursor=...` | 分页查看错题及相关证据 |
| `GET /api/v1/learners/:id/reviews` | 获取服务端计算的到期任务 |
| `GET /api/v1/learners/:id/changes?cursor=...` | 增量同步状态与删除标记 |
| `POST /api/v1/learners/:id/imports` | 家长指定目标档案后导入旧本机记录 |

每个涉及 learnerId 的接口都根据已认证 accountId 查询归属关系；猜到 ID 不能读写其他家庭的数据。服务端对批大小、分页和载荷大小设上限。学习端无后台发布权限，内容编辑人员默认无权浏览家庭学习记录。

## 8. 现有数据迁移与维护

目前 `Attempt` 只记录对错，没有保存所选答案、完整题目呈现，而且每个字只保留最近 30 条。迁移只能保留实际存在的记录，不能恢复已丢弃的历史，更不能编造孩子当时选了哪个字。

首次登录由家长选择“将这台设备上的记录导入哪个孩子”。服务端验证来源格式与版本，按导入指纹去重；记录 source=legacy_import，历史对错标为旧版自报证据，与服务端判题区分。保留已完成课程、恢复位置和可验证字段；不把导入一次当成一次新练习，也不凭历史布尔值重建完整错题快照。

原始作答按明确的数据保留策略维护，不能继续把“每字最多 30 条”当作完整云端错题历史。初期无需分表；数据增长后按实际查询、容量和保留要求归档。提供家长导出、删除档案与删除账号能力，删除范围覆盖原始事件、派生状态及本机队列；备份恢复流程需避免重新激活已删除账号的数据。

数据库备份同时覆盖课程版本与学习表；素材另有版本备份。上线前实际执行恢复演练，验证孩子归属、错题引用、内容版本及继续位置一致，不把“有备份任务”当作可恢复证明。

## 9. 开发顺序与验收

在内容服务的表设计阶段一起确定档案、题目版本和稳定 ID，不等所有内容接口做完再临时加 userId。功能可分批交付：

| 任务 | 工作量初估 | 完成标准 |
| --- | --- | --- |
| ACC-01 登录、档案与权限 | 1–2 工作日 | 一个家长两个孩子互不串记录；另一账户无法越权；退出与会话撤销生效 |
| ACC-02 在线课次、作答、进度与错题 | 2–3 工作日 | 服务端判题、事务、两层去重；重练不改正式位置；错题能追溯题目版本 |
| ACC-03 离线队列、冲突与旧记录导入 | 2–3 工作日 | 弱网重试不重复计数；切账号不串队列；两设备冲突可恢复；导入两次不叠加 |
| ACC-04 数据管理及恢复验收 | 1–2 工作日 | 导出／删除、备份恢复、手机重新登录及跨设备继续通过 |

合计约 **6–10 工作日增量**，基于前后端分离方案已有 API、数据库和内容版本能力；与内容服务原估算 7–12 工作日合并，初步约 13–22 工作日。认证基础能力可与管理后台共用，具体排期在选定路线后去重；短信／邮件服务开通、素材返修及外部审校等待另计，不承诺上线日期。

必须覆盖：重复／并发／乱序提交、事务失败、跨家庭越权、多孩子切换、离线退出登录、内容改版与旧错题、辅助完成不当独立成功、日期跨日和设备时间异常、旧记录缺字段与重复导入。该设计不代表当前手机页面已经具备登录或云同步。


## 多客户端身份、同步与权益补充（2026-09-21）

家长 accountId 是跨端身份主体，learnerId 是孩子学习记录归属；不同设备和登录渠道不能各建一套独立学习身份。扩展 Identity 的 provider/subject 时，微信平台标识须在服务端验证后映射；绑定或合并已有账户要求验证双方身份，不能仅凭昵称、相同邮箱字符串或客户端传来的标识合并。微信标识之间的关联能力在小程序立项时核实。

网页 Cookie 与移动客户端凭据在认证适配层处理，统一提供当前账户与会话上下文。移动凭据须有过期、撤销、轮换及安全存储策略；退出、换账户、设备撤销后，隔离或清除对应缓存与待同步队列。服务端不信任客户端提交的 accountId、learnerId、correct 或权益状态。网页写请求仍防护 CSRF，同域不代表可以取消权限验证。

多客户端共用内容版本、题目呈现、clientEventId 去重、服务端修订号与冲突规则。音频播放、缓存和网络状态通过平台适配器提供；IndexedDB 是网页实现，不是小程序/App 的强制存储技术。恢复学习需保留课次、步骤、场景与布局；原始事件追加记录，不用不同设备的整份本地快照互相覆盖。

权益由家长账户持有；学习进度仍属于孩子。首期统一 `EntitlementService` 默认免费，不接真实支付。权益设计使用能力/内容范围、来源、有效期与可选额度，避免单一 isPaid；将账户状态、孩子数据归属、管理员角色与商业权益分别校验。

| 后续实体/接口 | 职责 | 首期范围 |
| --- | --- | --- |
| entitlement grants | accountId、功能/内容范围、生效/到期、来源及撤销状态 | 先定义服务契约与免费策略；真实授权持久化在付费阶段实施 |
| usage counters | 额度规则、计费主体、周期与使用量 | 契约区分按账户/按孩子；实际启用前确认产品规则 |
| billing events | 渠道事件 ID、验签结果、处理状态、关联授权 | 支付阶段建设；唯一约束去重，处理退款、撤销与乱序 |

权益判断覆盖受限课程包、素材访问及练习 API；额度扣减与业务成功保持事务一致，网络重试不得重复扣减。离线已下载内容不能保证即时收回：允许离线多久、到期后是否续学及跨端额度如何处理，在收费上线前明确；先前有效学习记录不因权益到期被删除。支付渠道仅更新统一权益，不把供应商字段散落到判题和进度逻辑中。

## ACC-01 实施落点

2026-09-21：邮箱认证使用 Better Auth，复用 Account／Identity／AuthSession；增加 Verification／RateLimit，身份密码由组件哈希处理。客户端不能指定 role 或档案归属。认证会话 30 天有效、按日续期，退出删除会话，重设密码撤销全部旧会话；没有启用 Cookie 会话缓存，服务端撤销立即生效。限流计数存 DB。当前选择的孩子保存在本机偏好中，必须属于服务端返回的档案列表。

首个孩子由家长输入昵称创建，默认选中首个档案；后续支持创建、重命名及切换，每户最多 8 个。暂不收集孩子实名、生日或头像，不提供删除档案以避免提前引入学习记录删除语义。

游客存档与每个账户／孩子的本机存档隔离；退出不删除游客或孩子存档。浏览器共享本机数据不等于服务器授权，服务端对每个 learnerId 重新核验 accountId。正式云同步、错题查询、游客历史导入仍分别属于 ACC-02／ACC-03。

## ACC-02 实施落点（2026-09-22）

在线学习采用服务端权威课次状态，追加而非整份覆盖学习记录。`LearningSession` 增加模式、当前位置、修订号、找字结果；`QuestionPresentation` 保留服务端产生的实际选项顺序及提示/播放状态；`Attempt` 保留实际选择、跳过、播放异常、目标字、任务类型和规则版本。新增 `LearnerLesson` 单独保存累计课程完成，重玩不会取消历史完成；`LearningSkill` 保存按字/任务的基础复习与证据摘要，`MistakeItem` 指向实际错误作答及原题目版本。

每个孩子的写入先锁档案行，再检查事件编号、课次归属与 expectedRevision，在同一事务中更新事实和汇总。相同事件同内容返回 duplicate；同编号不同内容返回 409。单个题目实例另有唯一作答约束，同一选择的语义重试返回原结果，不能改成另一个答案覆盖历史。客户端提交 correct 或 prompted 等计算字段会被拒绝；提示必须先通过事件保存。音频播放结果仍是客户端报告的事实，不等于服务端能验证孩子是否认真听完；网页听音题等待完整播放后才开放独立作答。

辅助、跳过、播放失败/未听音的回答保留事实，不增加知识性错误或独立成功。次日复习按孩子默认学习时区 Asia/Shanghai 和服务端时间计算；不信任客户端时间制造跨日成功。规则版本为 1，跨日和不同任务的证据分开解释；当前为基础次日队列，1/3/7 天和延后纠错仍在 DEV-22/23。

新增在线端点：`GET progress`、`POST sessions`、`POST events`、`GET mistakes`、`GET reviews`、`PATCH learning-settings`，均位于 `/api/v1/learners/:id/`，服务端校验当前账户归属，不给管理员跨家庭读取特权。API 与响应运行时校验位于共享 contracts，并补入 OpenAPI。此轮使用单事件在线提交，不宣称批量离线 events:batch、增量 changes 或 imports 已实现。

客户端从服务端读取位置、已选答案、选项顺序和找字结果。只读词句重练不提交学习事件；复习使用独立 mode，不移动正式课程继续位置。在线请求丢失响应时在当前页面保留同一事件以供重试，未收到确认不显示已保存；同课次旧修订冲突需读取最新状态，断网暂停云端写操作。

旧游客及孩子本机进度原样保留，不自动上传、删除或从布尔 correct 编造实际选择；新云端档案与旧存档并存，页面说明待后续导入。声音偏好和亲子观察仍在独立本机偏好键；家长开放全部课程在云端保存，但不增加完成记录。现阶段沿用整个档案固定一个内容版本，所有课次也各自绑定具体 LessonVersion；新旧课次跨发布版本并存、持久化 IndexedDB 队列和导入属于后续扩展。


## 课末综合练习的数据约束（2026-09-22 已确认，待 DEV-22）

读句子后的综合练习与课前回顾、回顾花园共用按 learnerId 隔离的候选、作答证据和调度。候选覆盖该孩子所有已学内容：本课目标、近期/反复错题、到期和证据不足的能力；按 targetId＋skillType 去重和检查最近练习，避免换入口马上重复。仍使用经审校且该孩子有权访问的题目及明确版本引用，不随当前发布指针静默替换历史题。

综合练习通常 3–5 题（本课重点 1–2＋历史错题/到期项 2–3），不足不凑数。服务端创建独立练习轮次，保存入口来源、本课关联、选题规则版本、固定题单与题目版本、当前题序和完成/退出状态；复用现有事件幂等与作答记录，刷新/跨设备续练保持同一轮次，不重新抽题。具体表和接口在 DEV-22 实现时确定；现有 ACC-02 的单题复习课次不代表已具备多题轮次。

正式课程完成、综合练习完成和能力证据独立：练习开始、跳过或退出不改变正式课次恢复位置，也不撤销完成及下一课开放。辅助、音频失败、跳过按既有事实规则记录；同日重复不制造跨日成功，不覆盖历史错误。同次纠错沿用 DEV-23 的间隔、辅助证据和本轮题量限制。ACC-03 提供通用同步基础，DEV-22 需补充轮次及选题快照的同步契约与恢复测试，不将课末练习错误归入已交付功能。

## ACC-03 实现协议（2026-09-22）

以下为本轮实现口径，覆盖前文草案中尚未具体化的同步规则：

- 联网先通过 `POST sync-streams` 用客户端生成的 streamId 幂等登记流，绑定已经建立的课次，预登记该课所有题目呈现与选项顺序。离线只能继续这一已准备课次；新建课次/切换未准备课次需联网。
- IndexedDB 按 accountId＋learnerId 保存已确认快照、本机预览、流和事件队列。事件有固定 clientEventId、stream 内连续 seq、expectedRevision、客户端报告时间与时区；先事务落盘再反馈。compare-and-swap generation 防止同一浏览器多标签互相覆盖。配额不足或事务失败不会推进界面。
- `POST events:batch` 每批最多 100 条。`buffered` 只表示已收到；缺序等待，不应用后续项。只有 `applied` 才移除本机项。`conflict/rejected` 保留原始提交并暂停该流，后续项不越过。重试同编号不同内容返回冲突；应用失败回滚该事件的全部业务写入，单独保留拒绝结果。
- 同课次冲突由家长选择“保留本机副本，继续云端位置”；原队列归档在 IndexedDB，服务端也保留已收到的冲突流。首版不自动重放冲突作答到新课次、不取最大步数合并。不同课次不覆盖原始事件；未同步队列始终归原账户/孩子，退出或登录失效后停止发送，原账号登录才重试。
- 客户端时间仅是报告值。服务端接收时间与学习时区决定本轮汇总；报告时间与接收时间相差至少两分钟的记录标记 timeTrusted=false，保留作答、错误事实与原报告，但不据此提升或回退已有掌握状态，也不制造跨日成功。不会把设备修改日期当成真实跨日。规则版本更新为 2；更精细的可信离线学习日证明与按可信发生时间重算可后续扩展。
- `GET changes?cursor=revision` 返回改变的顶层字段（变化的集合字段整体替换）及被新课次替代的旧课次 ID 删除标记；无变化返回空 patch，未知基线返回 reset 全量。服务端保存版本化摘要快照，避免靠 updatedAt 漏更。当前没有档案删除接口；将来接入删除功能必须补充档案级墓碑/本机清除规则，不能将这里的课次移除标记声称为账号删除已实现。
- 旧记录导入由家长中心选择来源（本机游客或当前孩子旧存档）、目标孩子并确认摘要。校验 schemaVersion/contentVersion、课程/字/步骤引用，1–3 版位置映射到原活动，服务端规范化指纹按目标孩子去重；原始数据以 legacy_import 保存。累计完成取并集，已有云端位置优先；不能合并不同内容版本的档案。旧布尔 correct 不转成 Attempt、不编造所选选项、不增加新独立掌握，原本机文件不删除。

本机缓存包括按版本的课程包、目录、已下载素材和当前家庭档案提示，不保存会话令牌。联网会重新认证；401 不回退成可同步身份。HTTPS/localhost 可用 Service Worker 缓存应用外壳（不缓存认证或私有 API）；局域网 HTTP 不支持该能力时，仅承诺已打开页面离线继续及联网后的持久队列恢复，不承诺完全断网重开网站。素材未缓存/播放失败按辅助状态处理，不算独立成功。

## R7 本地实现补充（2026-09-22）

前文 ACC-01/ACC-03 的“不提供档案删除”是当时阶段记录；当前已新增 `GET /account/export`、`GET /learners/:id/export`、`DELETE /account`、`DELETE /learners/:id`（均在 `/api/v1` 下）。导出使用一致性事务读取本人家庭，可选择孩子，包含版本绑定、原始事实、实际选项、派生状态、旧导入和同步记录；浏览器额外包入当前设备未同步/冲突副本。导出不含密码、身份/登录令牌和清理凭据，不把未确认事件描述成云端事实。

删除需要本人会话、可信 Origin、当前密码和精确目标文字，独立五分钟限流；账户和孩子按锁顺序串行处理。孩子硬删除其学习关系，兄弟姐妹保留；账户额外撤销全部会话及身份。共用教学内容作者关联匿名化。每次删除留下 `DataDeletion` 最小凭据，并在数据库外持久化先行日志；恢复/启动即使已存在同一凭据也再次移除恢复出来的目标，不能因“处理过”而跳过。

`GET /me` 增量增加 `deletedLearnerIds` 与账户 `cleanupToken`，原 v1 字段与旧事件接口保持。孩子旧请求返回 410，非本人家庭仍为 404。账户被删后会话返回 401；设备以此前持有的随机 UUID 清理凭据调用 `POST /deletion-status`，仅获得 deleted 布尔值，不能用于登录、读取档案或修改数据。服务端只在最小删除凭据中保存其 SHA-256；普通会话过期不触发队列删除。

IndexedDB 删除标记与队列/冲突副本清除在同一事务执行，旧页面的后续 CAS 写入或冲突存档会检查标记并拒绝重建。跨设备下一次联网/前台检查时清理，游客记录独立，导出文件由家长管理。没有新版清理凭据的旧设备需要手工清除缓存，服务端仍拒绝其旧写入。备份恢复必须应用最新外部删除凭据且撤销所有恢复会话，详见《部署与维护》。

当前验证是本地 API/浏览器上下文和生产认证配置断言，没有真实 HTTPS 手机或新客户端运行证据。正式验收待环境，不标记小程序/App 已兼容。

R7 回归补充：`changes` 查询不再写入 ProgressSnapshot，避免并发首次读取争抢同一主键；缺少基线返回全量 reset，原始事件和学习变更仍在孩子锁下保存同修订快照。
